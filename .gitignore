# Dependencies
node_modules/

# Build outputs
dist/
dist-app/
dist-ssr/

# Caches
.turbo/
.tmp/
.cache/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / secrets
.env
.env.local
*.local

# OS / platform glitches
nul

# Test / debug artifacts
*.png
!public/*.png
!docs/**/*.png
console-messages.json
playwright-report/
test-results/
.playwright-mcp/

# Agent / AI tool scratch (kept out of the public repo)
.automaker/
.bolt/
.claude/worktrees/
.codex-main-push/
.opencode/
claude-code-plans/
codex-plans/
tasks/
tasks/lessons.md

# Session / report files that may carry secrets
session-ses_*.md
pathlens-report-*.json

# Distribution archives
*.zip
angriff36-manifest-*.tgz
junk/
scripts/

# Nested git worktrees (registered inside the repo). The superproject's
# `git add -A` does NOT auto-exclude these, so they must be ignored here.
# This has no effect on the agents working inside them — each worktree
# commits to its own branch via .git/worktrees/<id> metadata.
.worktrees/

# Local debug / verification scratch (root-level only)
/debug-*.mjs
/debug-*.mts
/debug-*.ts
/verify-aggregates.mts
/constraint-test-verify.spec.ts

# Compiled JS emitted next to TS sources under src/ (build output, not source).
# Only ir.js and projections/interface.js are intentionally tracked.
/src/**/*.js
!/src/manifest/ir.js
!/src/manifest/projections/interface.js
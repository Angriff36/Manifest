# Manifest Language (VS Code)

**Parked (2026-07-15):** in-repo extension only (`"private": true`).
Not published to the VS Code Marketplace. Sideload / develop from
`packages/vscode-extension` until an explicit unpark + publish decision.

Language support for the Manifest DSL — syntax highlighting, diagnostics,
autocomplete, and more.

# @manifest/mcp-server

**Parked (2026-07-15):** in-repo workspace package only (`"private": true`).
Not published to npm. Use from this monorepo / local path until an explicit
unpark + publish decision.

MCP server for the Manifest DSL — compile, execute, validate, and introspect
Manifest programs.

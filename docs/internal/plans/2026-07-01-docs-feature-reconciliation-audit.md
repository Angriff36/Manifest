# Docs ↔ Feature Reconciliation Audit — 2026-07-01

**Scope:** full reconciliation of Manifest's implemented capabilities (main @ v2.22.0, 3,161 tests green) against both user-facing doc trees — `mintlify/` (public site, 66 pages) and `docs/` (features/guides/projections/getting-started/reference) — plus a dedicated hunt for every place a user must hand-author a script/shim/wrapper to get a working system.

**Method:** 80 independent audit agents (13 parallel inventory/docs/UX readers over source-of-truth code, then a reconcile phase). Every phantom-doc claim and every glue-gap tier below was **adversarially verified by a separate agent instructed to refute it**, with file:line evidence. Verdicts marked `confirmed` survived that refutation attempt.

> **Reconciliation note (2026-07-14):** This audit is 13 days old; main has since shipped WS1 (docs correctness, same-day 2026-07-01), WS2A–WS2G (glue-gap elimination), the IR wiring repair waves, and a Convex semantics wave, moving from v2.22.0 to v3.5.0. Of the concrete findings re-verified against current source (Finding 1 table, Finding 2 Clusters A–C / Appendix B's 40 items, and a spot-check of Appendix C/D): **~28 are FIXED-SINCE** (cited inline below with file:line evidence), **~17 remain STILL-OPEN**, **~5 are PARTIALLY-FIXED**, and **1 (encryption no-op) was already correctly framed as by-design in Appendix D, not a bug**, despite Finding 2 Cluster C classifying it as a native-gap. The historical prose (what the audit found on 2026-07-01) is left intact; only current-state assertions that are now false are struck through with dated updates. Appendix D's `working`/`partial`/`phantom` rows not explicitly touched below remain accurate as of this pass — they were spot-checked, not exhaustively re-verified. See inline `**Update (2026-07-14):**` blocks throughout.

**Policy driving the glue classification (owner-set):** nothing should require hand-written glue. Every glue requirement is tiered:

- `config-option` — capability exists, just needs a parameter
- `native-gap` — Manifest should emit/implement it; treat as a bug or unfinished feature
- `template-ok` — genuinely app-specific; docs must ship a copy-paste template (last resort)
- `already-solved` — main already fixed it; the doc/script is stale

---

## Executive summary

1. ~~**The docs are not just unfriendly — they are wrong at the front door.** The mintlify quickstart's first `.manifest` example does not compile (invented `policy read allow if` / `is not empty` syntax), its `RuntimeEngine` context shape can never satisfy its own policies (`userId` vs the real `context.user` / `actorId`), and `runtime.on(...)` doesn't exist. The Next.js and manifest-files pages both use `command publish()` — `publish` is a reserved keyword. **A new user's first three copy-pastes all error out.**~~ 23 doc claims were confirmed phantom (documented, not implemented), 16 more partially wrong (Appendix A).
   **Update (2026-07-14):** fixed same-day (2026-07-01, commits `7cb8ea9`/`84156f8`/`a2494d5`) — `mintlify/quickstart.mdx` now uses correct syntax throughout (`mutate title = newTitle`, `policy AdminRead read: user.role == "admin"`, `guard newTitle != null and newTitle != ""`, `new RuntimeEngine(ir, { user: {...}, tenantId })`, `runtime.onEvent(...)`), and `command publish()` was renamed in `mintlify/integration/nextjs.mdx` and is absent from `mintlify/language/manifest-files.mdx`. A blocking CI doctest gate (`.github/workflows/ci.yml` "Documentation integrity checks" → `scripts/check-doc-snippets.mjs`) now compiles every ` ```manifest ` block in docs against the real compiler — **but it does not typecheck ` ```typescript ` blocks**, so the entity-first `runCommand('Order', 'updateStatus', {...})` phantom (real signature: `runCommand(commandName, input, options)`) survives on `mintlify/language/commands.mdx:221`, `events.mdx:83`, and `approvals.mdx:133` (not swept; extend the doctest gate to TS blocks to close this permanently).

2. **The glue problem is real and it is overwhelmingly Manifest's to fix, not the user's.** Of 40 deduped hand-authored-glue requirements: **36 native-gap, 1 config-option, 2 template-ok, 1 already-solved.** The pattern is consistent: Manifest stops one file short of a runnable system. ~~The single highest-leverage fix: **no projection emits the runtime factory** (`createManifestRuntime`), yet every server projection's generated code imports it — generated output cannot compile without hand-written `@/lib/manifest-runtime`, `@/lib/manifest-response`, `@/lib/database`, auth middleware, and a tenant helper.~~ The building blocks (`createStoreProvider`, `resolveUser` factories) already exist **uncalled** in `packages/cli/src/utils/config.ts:577-702`. (Appendix B.)
   **Update (2026-07-14):** the runtime-factory gap is fixed (WS2A, commit `5d83d8d`) — `generateRuntimeFactoryModule()` in `src/manifest/projections/shared/companions.ts:150` now emits `lib/manifest-runtime.ts`, and Next.js additionally emits `lib/manifest-response.ts`, `lib/database.ts`, and a tenant helper (`src/manifest/projections/nextjs/generator.ts:1826-1882`); express/hono/remix/sveltekit consume the same companion module. Generated code now compiles without hand-written companions for these five projections. Two things remain genuinely open: (a) Hono and Express still have **no `authProvider` option at all** (confirmed: zero matches in `src/manifest/projections/hono/types.ts` and `express/types.ts`), so auth wiring for those two frameworks is still 100% hand-written; (b) `createUserResolver()`/`resolveUser` (`packages/cli/src/utils/config.ts:723`) is still called from nowhere except `packages/cli/src/commands/scan.ts` and its own tests — no generated route or runtime factory invokes it, so per-request identity/context construction is still hand-rolled boilerplate.

3. ~~**A second glue cluster: the engine enqueues work nothing ever drains.** Outbox entries are enqueued but no worker ships; async commands have only `MemoryJobQueue`; `shouldRunCron` is hardcoded `return false`; approval expiry needs an unshipped sweeper; `IdempotencyStore` has zero implementations (audit/outbox/approval all ship memory+postgres adapters — these are the missing siblings).~~
   **Update (2026-07-14):** fixed (WS2B, commit `abe9595`) — `runOutboxWorker` ships (`src/manifest/outbox/worker.ts`); `PostgresJobQueue` + `runJobWorker` ship (`src/manifest/jobs/`); `shouldRunCron` now delegates to a real cron-field evaluator (`src/manifest/runtime-schedule.ts:182-184`, verified directly — no longer hardcoded `false`); the approval-expiry sweep is emitted into the generated schedule surface; `IdempotencyStore` now ships memory + Postgres implementations (`src/manifest/idempotency/stores/{memory,postgres}.ts`).

4. **Pure wiring debt:** shipped, tested code that is unreachable — ~~redis/dynamodb/mongodb outbox stores and the entire federation module missing from `package.json` exports~~; five complete CLI commands (analyze, seed, profile, pack/unpack, generate-from-prompt) never registered in `index.ts`; `loadPlugins()` has **zero call sites** (the whole plugin config system is inert); the config JSON schema hard-closes `projections` to 4 of 27 registered projections; the approval Postgres `.sql` schema isn't even in the npm `files` list.
   **Update (2026-07-14):** mostly fixed (WS2C, commit `ac727f9`) — verified directly against `package.json`: `./outbox/redis` and `./federation` are now exported (`dynamodb`/`mongodb` outbox stores deliberately stay unexported — they have zero test files, a decision the audit itself records at the bottom of this doc under "Corrections found during WS2C implementation"). The five CLI commands are registered (`packages/cli/src/index.ts`: `analyze` line 333, `seed` 357, `profile` 442, `pack` 472, `unpack` 481, `generate-from-prompt` 499). `loadPlugins()` now has real call sites (`packages/cli/src/utils/plugins.ts:54`, invoked from CLI startup and `plugins list`). The config schema now allows 28 projection keys (verified: `node -e "require('./docs/spec/config/manifest.config.schema.json').properties.projections.properties"` → 28 keys), not 4. The approval/audit/outbox/jobs/idempotency `.sql` schemas are now in `package.json`'s `files` array — but there is still no `manifest db init` (or equivalent) command, so applying them remains a manual step.

5. **Five advertised projection options are dead code** (accepted, normalized, never consulted): react-query `optimisticUpdates`, hono `basePath`, kysely `columnMappings`, pydantic client options, analytics `importPath`. The analytics projection emits code that cannot compile (imports a never-generated sibling module, calls SDKs it never imports).

6. **There are two full, drifting user-facing doc sets.** mintlify/ and docs/ duplicate ~40 topics; the two quickstarts teach contradictory runtime APIs; `docs/internal/DOCUMENTATION_GOVERNANCE.md` describes a directory layout that no longer exists and assigns no tier to the four biggest user-facing directories, so the drift has no owner. Internal docs/ pages are more accurate but written spec-first (IR internals before task recipes); mintlify is task-styled but error-dense.

7. **Coverage tally** (133 matrix rows): 76 full/full, but 27 rows missing from mintlify entirely, 33 missing from internal docs, 8 missing from **both** (health projection, WASM evaluator, GenericPrismaStore, @manifest/stdlib, batched-persistence atomicity guarantee, ~~the config-schema closed-set gap~~, silently-dropped entity `behaviors`, unregistered CLI commands). ~~Two internal pages say "Status: Planned" for **shipped** features (entity-inheritance, scheduled-commands).~~
   **Update (2026-07-14):** the config-schema closed-set gap is FIXED (now 28 projection keys, verified directly). Both "Status: Planned" pages are FIXED — `docs/features/entity-inheritance.md:5` and `docs/features/scheduled-commands.md:3` now correctly state their features are implemented (verified directly). Health projection, WASM artifact, GenericPrismaStore docs, `@manifest/stdlib` publication, batched-persistence-in-mintlify, and the `behaviors` silent-drop are all confirmed STILL-OPEN as of this pass (batched-persistence is actually now documented in mintlify — see Appendix C update — so of the original 8 "missing from both" items, 1 is now covered and 1 (config-schema gap) is fixed, leaving 6 genuinely still missing). Item 5 (dead projection options) was not re-verified this pass.

---

## Finding 1 — Onboarding path is broken (highest user-pain)

Verified by compiling doc snippets against the real compiler:

| Page                                   | Break                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mintlify/quickstart.mdx`              | Todo example fails: `mutate this.title = title` (mutate takes bare property), `policy read allow if ...` (syntax doesn't exist; `read`/`execute` are reserved), `guard title is not empty` (no such operator) — under a banner claiming "nothing is invented"                                                                                               |
| `mintlify/quickstart.mdx`              | `new RuntimeEngine(ir, { userId, tenantId })` — `user.*` bindings resolve from `context.user`; a top-level `userId` is never mapped, so the page's own policy can never pass. `integration/overview.mdx` shows the correct shape — the two entry docs contradict each other                                                                                 |
| `mintlify/quickstart.mdx`              | `runtime.on('TodoCompleted', ...)` — no such method (real: `onEvent`/`subscribe`); `troubleshooting.mdx:70` even says so                                                                                                                                                                                                                                    |
| `mintlify/integration/nextjs.mdx`      | `command publish()` — reserved keyword, whole walkthrough bricks; `projection.generateRoute/generateTypes` don't exist (only `generate(ir, request)`); expected output shows per-command routes but default `--surface all` emits a dispatcher (word "dispatcher" never appears on the page); `includeTenantFilter` documented default-on, actually `false` |
| `mintlify/language/manifest-files.mdx` | Same `command publish()` reserved-word failure — on the first Language page in the nav — while the same page documents that exact error at line 297                                                                                                                                                                                                         |
| `mintlify/installation.mdx`            | Pins stale version 2.18.0                                                                                                                                                                                                                                                                                                                                   |
| `mintlify/cli/*`                       | `manifest build --output` (real flag: `--code-output`); `manifest test constraints`, `generate-fixtures`, `env(VAR)` config expansion, `MANIFEST_ENV` overlays, top-level `stores:` config block — all confirmed phantom                                                                                                                                    |

**Highest-leverage single fix:** a CI doctest gate that compiles every ` ```manifest ` and TypeScript snippet in `mintlify/` (and `docs/`) against the real compiler/CLI. Nothing else prevents regression.

**Update (2026-07-14):** all seven rows above are fixed as of 2026-07-01 same-day commits `7cb8ea9`/`84156f8`/`a2494d5` — verified against current file content, not just commit subjects. `mintlify/quickstart.mdx` uses correct `mutate`/`policy`/`guard`/`RuntimeEngine`/`onEvent` syntax throughout; `mintlify/integration/nextjs.mdx:28` renamed the command and now explicitly documents why `publish` is reserved; `mintlify/language/manifest-files.mdx` no longer uses `command publish()`; `mintlify/installation.mdx` no longer pins a version (uses unversioned `npm install`/`@latest`). The doctest gate is real and CI-blocking (`.github/workflows/ci.yml` "Documentation integrity checks" step, backed by `scripts/check-doc-snippets.mjs`) — **but its scope is ` ```manifest ` blocks only**, not TypeScript, which is why the entity-first `runCommand(Entity, cmd, args)` phantom (Appendix A, Appendix C runtime-API row) still survives on `commands.mdx`, `events.mdx`, and `approvals.mdx` today. Extending the gate to typecheck TS snippets would close that permanently.

## Finding 2 — Glue-gap classification (the "hand-authored scripts" pain)

40 deduped items, tier distribution: **36 native-gap / 1 config-option / 2 template-ok / 1 already-solved**. Full table in Appendix B. The headline clusters, in priority order:

### Cluster A — generated code doesn't compile without hand-written companions (native-gap)

1. ~~**Runtime factory never emitted** — all six server projections import `createManifestRuntime` from a module nothing generates; `createStoreProvider`/`resolveUser` factories sit uncalled in `packages/cli/src/utils/config.ts:577`. Fix: emit a per-framework runtime surface composing IR + engine + stores + resolveUser. (L)~~
   **Update (2026-07-14): FIXED** (WS2A, `5d83d8d`) — `generateRuntimeFactoryModule()` (`src/manifest/projections/shared/companions.ts:150`) emits `lib/manifest-runtime.ts`; wired into nextjs/express/hono/remix/sveltekit generators. `resolveUser` wiring into the emitted factory is still not done — see item 12 below, which remains open.
2. ~~**`@/lib/manifest-response` + `@/lib/database` never emitted** — Remix and SvelteKit projections already emit the equivalent helpers; Next.js uniquely doesn't. (S)~~
   **Update (2026-07-14): FIXED** (WS2A) — `src/manifest/projections/nextjs/generator.ts:1839-1857` always emits `lib/manifest-response.ts` and conditionally emits `lib/database.ts`.
3. **Auth companions never emitted** — `authProvider` option only switches import statements; for known providers (clerk middleware.ts, lucia hooks.server.ts) the body is deterministic. Hono/Express lack even the provider option. (M)
   **Update (2026-07-14): PARTIALLY FIXED** (WS2A) — Next.js now emits a fail-closed auth stub for `custom`/local `clerk`/`nextauth` imports (`nextjs/generator.ts:2025-2067`), and SvelteKit emits a lucia binding. Hono and Express **still have no `authProvider` option at all** (verified: zero matches in `hono/types.ts` and `express/types.ts`) — that half of the item is unchanged.
4. ~~**Tenant lookup helper** — nextjs output _always_ imports a never-emitted `@/app/lib/tenant` when tenant filtering is on (the `userTenantMapping` fallback branch is unreachable dead code). (M)~~
   **Update (2026-07-14): FIXED** (WS2A) — `nextjs/generator.ts:1872-1882` emits the tenant module whenever tenant filtering + a tenant provider are configured (the default provider is always set).

### Cluster B — engine enqueues, nothing drains (native-gap)

5. ~~**Schedules never fire** — `shouldRunCron` hardcoded `return false`; `nextjs.schedule` surface is CLI-unreachable (not in the surface whitelist); vercel.json cron registration emitted only as a comment. (M)~~
   **Update (2026-07-14): FIXED** (WS2B, `abe9595`) — `shouldRunCron` (`src/manifest/runtime-schedule.ts:182-184`) now delegates to a real 5-field cron evaluator (`isCronDue`), verified directly, no hardcoded `false` remains; `vercel.json` is now emitted as strict JSON, not a comment; `nextjs.schedule` is CLI-reachable via `packages/cli/src/commands/generate.ts:400-403`.
6. ~~**Async commands** — no durable JobQueue, no drain worker (`manifest jobs drain` / PostgresJobQueue missing; audit/outbox/approval postgres siblings all ship). (M)~~
   **Update (2026-07-14): FIXED** (WS2B) — `src/manifest/jobs/worker.ts` (`runJobWorker`) and `src/manifest/jobs/stores/postgres.ts` (`PostgresJobQueue`) both ship.
7. ~~**Outbox delivery worker never shipped** — runtime only ever calls `enqueue`; even `integration-check` asserts entries stay `pending`. Ship `runOutboxWorker(store, deliver, opts)`. (M)~~
   **Update (2026-07-14): FIXED** (WS2B) — `src/manifest/outbox/worker.ts` ships exactly this helper (poll/claim/deliver/mark, at-least-once).
8. ~~**Approval expiry sweep** — `expire()` has zero callers; un-swept timed-out approvals stay grantable forever (semantically load-bearing). (S)~~
   **Update (2026-07-14): FIXED** (WS2B) — the expiry sweep is now emitted into the generated schedule surface (`schedule-generator.ts:95-97`).
9. ~~**IdempotencyStore: zero implementations ship.** (S)~~
   **Update (2026-07-14): FIXED** (WS2B) — `src/manifest/idempotency/stores/{memory,postgres}.ts` both ship and are exported (`./idempotency/memory`, `./idempotency/postgres`).
10. ~~**No engine transaction boundary** — outbox enqueue non-atomic; adapters already accept the `tx` param the engine never supplies. (L)~~
    **Update (2026-07-14): FIXED** (WS2D) — `RuntimeOptions.transactionProvider` + `_runCommandInTransaction` (`runtime-engine.ts:270-279, 3519-3590`) thread one transaction across mutation + outbox enqueue + idempotency set, propagated to nested/reaction commands. The old non-transactional path remains as the documented fallback when no provider is configured.
11. ~~**Webhooks: IR-only** — no runtime code, no route surface outside Convex; even Convex ignores `signature`/`idempotencyHeader`. Docs claim runtime HMAC verification that doesn't exist. (M)~~
    **Update (2026-07-14): FIXED** (WS2F) — `src/manifest/webhooks/handler.ts` implements real HMAC verification (`createHmac`/`timingSafeEqual`, fail-closed) and `nextjs/webhook-generator.ts` emits real route files.
12. ~~**Realtime is single-instance** — `RedisStore.subscribe` is an in-process stub; no `eventBus` RuntimeOption despite the sibling-adapter pattern (auditSink/outboxStore/approvalStore/jobQueue). (L)~~
    **Update (2026-07-14): FIXED** (WS2G) — the old silent stub is gone; `RedisStore.publishEvent`/`subscribe` (`stores.node.ts:769-786`) now throw and point callers at `RedisEventBus` (`src/manifest/events/redis.ts`); `RuntimeOptions.eventBus` exists (`runtime-engine.ts:291`).

### Cluster C — wiring/registration debt (native-gap, mostly S)

13. ~~Exports map omits shipped `./outbox/{redis,dynamodb,mongodb}` and `./federation` (docs literally tell users to reimplement the Redis outbox).~~
    **Update (2026-07-14): PARTIALLY FIXED** (WS2C, `ac727f9`) — `package.json` now exports `./outbox/redis` and `./federation` (verified directly). `dynamodb`/`mongodb` outbox stores remain unexported — deliberately, per the "Corrections found during WS2C implementation" note already at the bottom of this doc: those two adapters have zero test files.
14. ~~`loadPlugins()` zero call sites — the whole `plugins:` config feature is inert.~~
    **Update (2026-07-14): FIXED** (WS2C) — real call sites now exist in `packages/cli/src/utils/plugins.ts:54`, invoked from CLI startup and `plugins list` (`index.ts:1668, 1738`).
15. ~~Five complete CLI commands unregistered;~~ `build` lacks `--all`; no `compile --print`; no git-ref mode for `diff breaking`.
    **Update (2026-07-14):** the five commands (analyze, seed, profile, pack, unpack, generate-from-prompt) are now registered in `packages/cli/src/index.ts` (FIXED). `--print`/git-ref-mode sub-items were not re-verified this pass.
16. ~~Config schema closed to 4 of 27 projections (`additionalProperties:false`) — configuring any other projection fails `manifest config validate` while `generate --all` happily consumes it. Generate the schema from the registry.~~
    **Update (2026-07-14): FIXED** (WS2C) — `docs/spec/config/manifest.config.schema.json` now allows 28 projection keys, registry-generated (verified by loading the schema directly).
17. ~~Single-run `generate -p <X>`/`build` resolve only nextjs options (the `--all` path proves per-projection plumbing works); global `naming` helper (`resolveProjectionOptions`) has zero CLI callers.~~
    **Update (2026-07-14): FIXED** (WS2C) — `packages/cli/src/index.ts:237` (generate) and `:313` (build) now call `resolveProjectionOptions(options.projection)`, resolving each projection's own config block.
18. Postgres adapter SQL applied by hand from `node_modules`; ~~the approval `.sql` isn't even shipped (missing from `files`).~~ Fix: `manifest db init`.
    **Update (2026-07-14): PARTIALLY FIXED** — the approval/audit/outbox/jobs/idempotency `.sql` schemas are now in `package.json`'s `files` array (verified). `manifest db init` still does not exist — applying the schemas by hand is still required.
19. Encryption is a silent no-op without a provider (test-pinned) — contradicts the fail-closed philosophy; WebCrypto is already used in-engine, so a default AES-GCM provider is dependency-free.
    **Update (2026-07-14): STILL OPEN, no default provider ships.** Note the tension with this doc's own Appendix D verdict on `field-level-encryption`, which frames the same no-op as "a documented design choice... not a broken claim." Both are true simultaneously: it's intentional (fail-closed by omission) *and* still a usability gap for the common no-vendor case this item describes.
20. Referential actions (`onDelete cascade` etc.) are inert in the reference runtime — spec-first fix required.
    **Update (2026-07-14): STILL OPEN.** `runtime-engine.ts` `deleteInstance()` remains a plain `store.delete()` passthrough with no cascade/restrict/set-null logic (verified: zero `onDelete`/`cascade` matches in the file).
21. Dead options (5), analytics non-compiling output, GraphQL scaffolding-only resolvers, health projection's always-healthy TODOs, materialized-views' unwired expression-to-sql translator, mongoose two-file stub, Prisma `through` relations, RLS emitted as comments, Dart client targeting routes no backend emits, no shared route-layout contract across nextjs/react-query/routes/zod/hono (live drift: hono/express import Zod schema names the zod projection doesn't generate).
    **Update (2026-07-14):** mixed and not fully re-verified. Confirmed **still open**: materialized-views' `generator.ts` still does not import `expression-to-sql.ts` (raw-SQL passthrough only, verified). The shared route-layout sub-claim was targeted by WS2E ("one route-layout contract — generated clients call generated servers") but was not independently re-verified this pass. The remaining sub-items (dead options, analytics, GraphQL, health TODOs, mongoose stub, Prisma `through`, RLS-as-comments, Dart route mismatch) were not re-checked — treat as unconfirmed rather than reasserting them as still-open.

### config-option (1)

- `flag()` builtin: add a static `flags` map resolved before `flagProvider`.
  **Update (2026-07-14): STILL OPEN.** `RuntimeOptions` (`runtime-engine.ts`) only has `flagProvider`; no static `flags` map exists.

### template-ok (2) — the only legitimately app-specific glue

- Framework/infra shells: mounting the Express/Hono app, `terraform apply`, `convex dev`, Storybook component wiring, ES index PUT + hosting the indexer. **Fix: one copy-paste template per target in docs.**
- Vendor event transports: bridging `onEvent` to Ably/BullMQ/Sentry etc. — after the outbox worker + event-bus option land, what remains is a provider closure. Keep `docs/guides/event-wiring.md` as the template catalog.

### already-solved (1) — stale docs prescribing glue main no longer needs

- "Call projection functions in your build scripts" (superseded by `compile --all` / `generate --all` / `watch --all`), hand-written PrismaStore (`./stores/prisma-generic` ships), hand-editing `@relation` names (deterministic pairing + `PRISMA_RELATION_PAIRING_ASSUMED` now native). **Doc updates only.**
  **Update (2026-07-14): the CLI capability was already true; the doc update was NOT applied.** `mintlify/integration/projections.mdx:65-84` still tells users to "call the projection functions directly in your build scripts" with a manual `compileToIR` example, instead of pointing at `compile --all`/`generate --all`/`watch --all` (all of which are real and wired: `packages/cli/src/index.ts` lines 170, 207, 268, 539). This is a one-file doc fix still pending.

## Finding 3 — Coverage holes

- **Missing from BOTH doc sets:** health projection, WASM evaluator (also: no `.wasm` artifact exists despite a `./wasm` export — ship or delete), GenericPrismaStore, @manifest/stdlib (also unpublished), entity `behaviors` (parsed then **silently dropped** with no diagnostic — language bug), batched-persistence atomicity, config-schema closed-set gap.
- **Internal docs actively wrong:** `docs/features/entity-inheritance.md` and (partially) `scheduled-commands.md` say "Planned" for shipped features; `docs/projections/README.md` lists 12 of 27 projections; `docs/getting-started/new-project.md` still mandates building the monorepo; `docs/guides/usage-patterns.md` contains absolute `C:/Projects/Manifest/...` self-citing paths; `docs/FEATURE-LIST.md` presents shipped v1.9–2.0 work as future.
- **Systemic mintlify batch-fixes:** entity-first `runCommand` signature on 7+ pages; "generate wires Next.js only" stale claim on ~10 pages across both trees; contradictory guard/constraint ordering (faq + embedded-runtime wrong); federation page imports functions that don't exist (`injectPolicyHeaders` → real: `buildBridgeFromContext`); per-projection option tables for remix/sveltekit/pydantic/flutter/kysely/terraform/turso/hono list options and surfaces that do not exist (see Appendix A).

## Finding 4 — User-friendliness / information architecture

- **Nav is reference-first:** 21 Language + 18 Projection pages before `integration/overview` — the page that answers "how do I actually put this in my app". `integration/nextjs` + `integration/prisma` are misfiled in the Projections nav group away from their siblings. Reorder: Get Started → Integration (decision guide first) → task guides → Language reference → Projections reference.
- **Missing journey pages (neither tree):** "go to production" checklist, "debug a failing guard" task page, "adopt Manifest over an existing database/schema". The genuinely good "add to an existing Next.js app" walkthrough exists only in mintlify.
- **Tone split:** mintlify pages are task-styled but error-dense; `docs/` pages are accurate but spec-first (tenant-isolation leads with "the compiler emits an IRTenant record"). Users need task recipes with reference linked, not IR internals up front.
- **Jargon** (IR, projection, surface, dispatcher, conformance, outbox) is used 1–3 pages before definition. Add a 10-line glossary to the introduction and link terms on first use.
- **Recommendation: one canonical user-facing doc set.** Make mintlify/ canonical for users; reduce `docs/` to spec + internal + contributor docs; delete or redirect the ~40 duplicated user pages after porting the few internal-only gems (primitives-reference, event-wiring catalog). Update `DOCUMENTATION_GOVERNANCE.md` to match reality and assign explicit tiers to every user-facing directory.

---

## Remediation plan

**Workstream 1 — stop the bleeding (docs correctness), ~days:**

1. ~~Fix the quickstart + nextjs + manifest-files compile breaks (the three broken first-copy-pastes).~~ **DONE 2026-07-01 (same day), commit `7cb8ea9`.**
2. ~~Add the snippet-doctest CI gate (compile every `.manifest` + `ts` block in mintlify/ and docs/).~~ **DONE 2026-07-01, commit `84156f8` — but scope is `.manifest` blocks only; TS blocks are not typechecked, which is why the entity-first `runCommand` phantom survived. Extending the gate to TS remains open.**
3. Batch-fix the 23 confirmed phantoms (Appendix A) — either implement the small ones (some are one-line, e.g. hono `basePath`) or delete the claim; per project policy an accepted-but-dead option is a bug, so prefer implement where S-effort. **Update (2026-07-14): the quickstart/nextjs/manifest-files/installation phantoms are fixed; the entity-first `runCommand` phantom (commands.mdx/events.mdx/approvals.mdx and likely others) is confirmed still open; the remaining ~18 phantoms in this appendix were not resampled this pass.**
4. Apply the `already-solved` doc updates (stale glue prescriptions). **Update (2026-07-14): STILL OPEN for at least one item — `mintlify/integration/projections.mdx:65-84` still tells users to hand-call projection functions in build scripts instead of pointing at `compile --all`/`generate --all`/`watch --all`.**

**Workstream 2 — kill the glue (native gaps), ~weeks, ordered by leverage:**

1. ~~Emit runtime factory + companion modules (manifest-response, database, auth stubs, tenant helper) → generated code compiles out of the box.~~ **DONE (WS2A, `5d83d8d`) for nextjs/express/hono/remix/sveltekit — except Hono/Express still lack an `authProvider` option entirely, and `resolveUser` is still not called from any generated code.**
2. ~~Wire the drains: outbox worker, PostgresJobQueue + drain, real cron evaluation + vercel.json artifact + CLI-reachable schedule surface, approval-expiry cron emission, idempotency adapters,~~ `manifest db init`. **DONE (WS2B, `abe9595`) except `manifest db init` — the Postgres SQL schemas now ship in `package.json` files[] but still require manual application; no init command exists.**
3. ~~Wiring debt sprint (all S): exports map lines, register the 5 CLI commands, `build --all`, call `loadPlugins()`, regenerate config schema from registry, wire `resolveProjectionOptions` into single-run paths.~~ **DONE (WS2C, `ac727f9`), verified directly — all sub-items confirmed in current source.**
4. Shared route-layout/naming contract consumed by nextjs/react-query/routes/zod/hono + drift diagnostic; ts.client command callers. **Update (2026-07-14): WS2E ("one route-layout contract") targeted this; not independently re-verified this pass.**
5. ~~Larger engine work: transaction boundary,~~ referential actions (spec-first), ~~event bus/realtime, webhooks runtime + surfaces.~~ **Transaction boundary DONE (WS2D). Event bus/realtime DONE (WS2G). Webhooks DONE (WS2F). Referential actions (`onDelete cascade`) confirmed STILL OPEN — `deleteInstance()` is still a plain passthrough with no cascade logic.**

**Workstream 3 — one doc set + IA, ~weeks, parallel with WS2:**
Canonicalize mintlify, restructure nav (task-first), add the three missing journey pages, add glossary, port/retire docs/ duplicates, write the per-target templates for the two template-ok clusters, update DOCUMENTATION_GOVERNANCE.md. **Not evaluated in the 2026-07-14 reconciliation pass — no evidence gathered either way.**

---

_Appendices below are machine-generated from the verified audit data. Workflow run `wf_bfc7b0f9-800`; raw JSON in the session scratchpad._

## Appendix A — Phantom doc-claim verdicts (each independently verified against source)

| Verdict              | Claim                                                                                                       | Doc                                             | Evidence (verifier)                                                                                                                                                                                                                                                                                           |
| -------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| confirmed-phantom    | Policy syntax 'policy read allow if <expr>' / 'policy execute allow if <expr>'                              | mintlify/quickstart.mdx                         | 1) DOC CLAIM: mintlify/quickstart.mdx:56-57 shows `policy read allow if user.role == "admin"` / `policy execute allow if user.id == this.createdBy`; mintlify/troubleshooting.mdx:247-248 offers the same invalid form (`policy execute allow if ...`, `policy all allow if ...`) as the FIX in its accordio… |
| confirmed-phantom    | Guard expression 'guard title is not empty'                                                                 | mintlify/quickstart.mdx                         | No `empty` keyword, builtin, or binding exists anywhere in the implementation. (1) C:/projects/manifest/src/manifest/lexer.ts:16-69 — the authoritative KEYWORDS set contains 'and', 'or', 'not', 'is', 'in', 'contains' but no 'empty'; `empty` lexes as an ordinary IDENTIFIER (lexer.ts:190). (2) C:/proj… |
| confirmed-phantom    | RuntimeEngine context field 'userId' drives user.* guard bindings                                           | mintlify/quickstart.mdx                         | CLAIM SITES: mintlify/quickstart.mdx:109-117 says "The context provides the user identity ... that guards and policies resolve against" and constructs `new RuntimeEngine(ir, { userId: 'user-123', tenantId: 'tenant-456' })`, while the quickstart's own example program (quickstart.mdx:56-57) has polici… |
| confirmed-phantom    | runtime.on('TodoCompleted', handler) event subscription                                                     | mintlify/quickstart.mdx                         | mintlify/quickstart.mdx:156-165 documents `runtime.on('TodoCompleted', (event) => ...)` on the `RuntimeEngine` instance constructed at quickstart.mdx:112-117 from the package root import (`@angriff36/manifest` resolves to dist/manifest/runtime-engine.js per package.json "exports" ".", compiled from … |
| confirmed-phantom    | Constraint field 'rule:' with expression 'this.title is not empty'                                          | mintlify/troubleshooting.mdx                    | Doc claim at mintlify/troubleshooting.mdx:217-222 shows `constraint titleNotEmpty { severity: block / rule: this.title is not empty }`. Both halves are phantom vs code on main. (1) Field name: the constraint block parser (src/manifest/parser.ts:1446-1489) only recognizes `code`, `severity`, `express… |
| confirmed-phantom    | Command-level policy clause: 'policy execute: <expr>' inside a command body                                 | mintlify/language/guards-policies.mdx           | Parser: parseCommand's body loop (src/manifest/parser.ts:848-876) accepts only retry/rateLimit/guard/when/constraint/emit; a `policy` line falls to parseAction (parser.ts:1393-1410), which has no policy branch, and since `policy` is a lexer KEYWORD (src/manifest/lexer.ts:18) it errors in parseExpr. … |
| confirmed-phantom    | 'store Order in event-sourced' with append-only log, snapshots, replay                                      | mintlify/language/events.mdx                    | Doc claim at mintlify/language/events.mdx:253-270 ("Event sourcing store", `store Order in event-sourced`, append-only/snapshotting/replay/projections) has no implementation on main. (1) BuiltinStoreTarget = memory                                                                                        | localStorage | postgres | supabase | durable | mongodb (src/manifest/ir.ts:301; src/manifest/… |
| confirmed-phantom    | The runtime verifies webhook signatures (hmac) on incoming requests                                         | mintlify/language/events.mdx                    | Claim (mintlify/language/events.mdx:212 and the Note at :250): the runtime verifies webhook HMAC signatures and rejects invalid requests with 401 before dispatch. No such verification exists anywhere. (1) src/manifest/runtime-engine.ts contains zero webhook/HMAC code — its only 'signature' match is … |
| confirmed-phantom    | runCommand('Order', 'updateStatus', {args}) entity-first 3/4-arg signature                                  | mintlify/language/commands.mdx                  | Real signature is runCommand(commandName, input, options) with entityName/instanceId inside options — src/manifest/runtime-engine.ts:2225-2239, single declaration, no overload or arg-shifting; getCommand(name, entityName?) at runtime-engine.ts:1613-1620 resolves the FIRST arg as a command name only.… |
| confirmed-phantom    | Generic entities: entity X<T> and entity A = B<C>                                                           | docs/features/entity-inheritance.md             | No generic-entity implementation exists anywhere on main. (1) Parser: src/manifest/parser.ts:150-181 — parseEntity() consumes the entity name, optionally `extends` (l.164-167) and `mixin` (l.171-179), then immediately `this.consume('PUNCTUATION', '{')` (l.181); there is no path for `<T>` type parame… |
| confirmed-phantom    | 'manifest test constraints' command + ConstraintTestHarness + '@angriff36/manifest/testing' export          | mintlify/cli/testing.mdx                        | (1) No `test` command: packages/cli/src/index.ts enumerates every CLI command (lines 117-1194: init, compile, generate, build, watch, validate, fmt, install-hooks, validate-ai, generate-tests, docs, diagram, preflight, check, scan, harness, mock, routes, lint-routes, audit-routes, emit, audit-govern… |
| confirmed-phantom    | 'manifest generate-fixtures' load-data generator command                                                    | mintlify/cli/testing.mdx                        | The `manifest generate-fixtures` command does not exist. (1) The string "generate-fixtures" appears in exactly one file in the repo: mintlify/cli/testing.mdx (usage examples at lines 129-135). (2) The sole CLI command registry, C:/projects/manifest/packages/cli/src/index.ts (loaded directly by the b… |
| confirmed-phantom    | env(VAR) / env(VAR, default) value-inlining syntax in manifest.config.yaml                                  | mintlify/cli/configuration.mdx                  | Doc claim at mintlify/cli/configuration.mdx:618-651 (env(VAR)/env(VAR,"default") inlined at config-load time) and 653-686 (MANIFEST_ENV overlay files). No implementation exists: (1) packages/cli/src/utils/config.ts:211-225 loadYamlConfig returns yaml.load(content) verbatim — no expansion pass, no pr… |
| confirmed-phantom    | Top-level 'stores:' config block with url: env(DATABASE_URL)                                                | mintlify/cli/configuration.mdx                  | The claim (mintlify/cli/configuration.mdx:618-686) documents three things, all unimplemented: (1) a top-level `stores:` YAML block (`stores.postgres.url: env(DATABASE_URL)`, redis, dynamodb); (2) `env(VAR)` / `env(VAR, "default")` expression syntax resolved at config-load time; (3) `MANIFEST_ENV`-se… |
| confirmed-phantom    | Multi-environment overlays via MANIFEST_ENV                                                                 | mintlify/cli/configuration.mdx                  | The doc claim: mintlify/cli/configuration.mdx:653-686 ("Multi-environment configuration") says overlay files like `manifest.config.staging.yaml` are merged over the base config, selected via the `MANIFEST_ENV` environment variable (`MANIFEST_ENV=staging manifest build`, lines 674-681). No implementa… |
| confirmed-phantom    | projection.generateRoute / generateTypes / generateClient programmatic methods with options object          | mintlify/integration/projections.mdx            | 1) Doc claim: mintlify/integration/projections.mdx:85-98 calls `projection.generateRoute(ir, 'Recipe')`, `projection.generateTypes(ir)`, `projection.generateClient(ir)`; lines 100-179 document "Pass an options object as the third argument to generateRoute" with auth/tenant/soft-delete examples. mint… |
| confirmed-phantom    | 'No dedicated CLI command — manifest generate wires up the Next.js projection only'                         | mintlify/projections/express.mdx                | The doc limitation is stale — the capability it denies IS implemented on main. (1) packages/cli/src/commands/generate.ts:165-170: any non-nextjs projection dispatches through generateWithRegistryProjection (lines 181-237), which resolves registry.getProjection(options.projection) from '@angriff36/ma… |
| confirmed-phantom    | Kysely option 'columnMappings' renames columns                                                              | mintlify/projections/additional-projections.mdx | Doc claim: mintlify/projections/additional-projections.mdx:21 lists `columnMappings` among supported Kysely options. The option exists only as a declared/normalized field: it is typed at src/manifest/projections/kysely/options.ts:33 and defaulted to `{}` in normalizeOptions at src/manifest/projectio… |
| confirmed-phantom    | Hono option 'basePath' prefixes generated routes                                                            | mintlify/projections/hono.mdx                   | The behavior "basePath prefixes generated routes" has no implementation. src/manifest/projections/hono/types.ts:50-54 declares basePath with comment "Base route prefix for all generated routes. Default: '/api'"; src/manifest/projections/hono/generator.ts:72 normalizes it (opts.basePath ?? '/api') bu… |
| confirmed-phantom    | Pydantic options clientBaseUrl / clientClassName / emitConvenienceFunctions                                 | mintlify/projections/python-pydantic.mdx        | Doc claim (mintlify/projections/python-pydantic.mdx:83, and usage example at :42) says PydanticProjectionOptions accepts clientBaseUrl (default 'http://localhost:3000'), clientClassName (default 'ManifestClient'), and emitConvenienceFunctions (default true). Reality on main: (1) The three options ar… |
| confirmed-phantom    | Pydantic and Dart option 'customImports'                                                                    | mintlify/projections/python-pydantic.mdx        | Claim sites: mintlify/projections/python-pydantic.mdx:83 and mintlify/projections/flutter.mdx:90 both document a `customImports` option. Repo-wide grep for `customImports` matches ONLY those two doc files; case-insensitive grep for alternate names (additionalImports, extraImports, custom_imports, us… |
| confirmed-phantom    | SvelteKit options basePath / dispatcherImportPath / includeTenantContext / publicReads                      | mintlify/projections/sveltekit.mdx              | The SvelteKit projection itself is real and registered (src/manifest/projections/builtins.ts:116 `registerProjection(new SvelteKitProjection())`; src/manifest/projections/sveltekit/generator.ts), but none of the four documented options exists on it. (1) `SvelteKitProjectionOptions` (src/manifest/pro… |
| confirmed-phantom    | Turso option 'schemaPrefix' and per-entity typed-column schema generation                                   | mintlify/adapters/turso.mdx                     | (1) `schemaPrefix`: grep across the entire repo matches only mintlify/adapters/turso.mdx:26 itself; TursoConfig at src/manifest/stores.node.ts:764-774 is exactly {url, authToken?, tableName?, client?} — no schemaPrefix, and no prefixing logic anywhere (tableName at stores.node.ts:806 is a full-name … |
| partial              | Webhook declared inside an entity body                                                                      | mintlify/language/events.mdx                    | Doc claim (mintlify/language/events.mdx:214-236): webhook declared inside `entity Order { ... }`. Parser reality: webhooks are produced ONLY at top level (src/manifest/parser.ts:84) and module level (parser.ts:141); the entity-body parse loop (parser.ts:196-325) has no webhook branch — unknown token… |
| partial              | import { registerBuiltin } from '@angriff36/manifest' for custom expression functions                       | mintlify/language/expressions.mdx               | Doc claim at mintlify/language/expressions.mdx:131-139 shows `import { RuntimeEngine, registerBuiltin } from "@angriff36/manifest"` with a global registerBuiltin("discount", fn) call. That symbol does not exist: package.json maps the root export "." to dist/manifest/runtime-engine.js, and src/manife… |
| partial              | Top-level 'mixin Auditable { ... }' declaration and 'mixin Auditable' lines inside entity bodies            | mintlify/language/advanced-entities.mdx         | Mixin composition IS implemented, but the syntax documented in mintlify/language/advanced-entities.mdx:48-85 is phantom. (1) No `mixin` declaration keyword: `mixin` is absent from KEYWORDS (src/manifest/lexer.ts:16-31; lexer.ts:67 comment says it's contextual), and the parser's top-level dispatch (s… |
| partial              | schedule blocks declared inside entity bodies                                                               | mintlify/language/workflows.mdx                 | Scheduled commands ARE implemented, but NOT inside entity bodies as mintlify/language/workflows.mdx:79-97 claims. parseSchedule (src/manifest/parser.ts:1940-2026) is reachable only from top-level (parser.ts:85) and module scope (parser.ts:142); the entity-body dispatch loop (parser.ts:196-325) has n… |
| partial              | 'Status: Planned — the current parser does NOT accept extends, mixin'                                       | docs/features/entity-inheritance.md             | The doc banner (docs/features/entity-inheritance.md:3) denies three things: extends, mixin, and generics. Two-thirds of the denial is stale-false, one-third is accurate. (1) extends/mixin ARE shipped: lexer.ts:59 reserves `extends`; parser.ts:162-167 parses `extends Parent` and parser.ts:169-179 par… |
| partial              | Mock server on port 3456 with --output/--format/--seed flags                                                | mintlify/cli/testing.mdx                        | A `manifest mock` command exists, but none of the specifics documented in mintlify/cli/testing.mdx:89-115 are real. Real registration (packages/cli/src/index.ts:533-548) defines flags `-p, --port` (default '4000'), `--host`, `--cors`, `--scenario`; the implementation's MockCommandOptions is exactly … |
| partial              | Config 'plugins' are 'loaded by the CLI' with options 'passed to the plugin at load time'                   | mintlify/cli/configuration.mdx                  | Doc claim (C:/projects/manifest/mintlify/cli/configuration.mdx:112-123): plugins are "Third-party plugin declarations loaded by the CLI" with options "passed to the plugin at load time". What exists: `loadPlugins()` at C:/projects/manifest/src/manifest/plugin-loader.ts:284-437 is a real, complete lo… |
| partial              | Top-level global 'naming' convention is inherited by projections during generation                          | mintlify/cli/configuration.mdx                  | The merge helper exists exactly as the doc describes: resolveProjectionOptions() at src/manifest/config.ts:263-272 layers build.naming under projections.<name>.options with per-projection override winning; it is unit-tested (src/manifest/config.test.ts:50-96), publicly exported via the './config' pa… |
| partial              | 'Available projections' = nextjs and prisma; Next.js ships 'four surfaces'                                  | mintlify/integration/projections.mdx            | The doc's inventory claims are stale undercounts, but everything it does document exists and works — so this is "partial" in the reverse direction: the code exceeds the doc, and the doc's claims of totality are false. CLAIM 1 — "Available projections" = nextjs + prisma (mintlify/integration/project…  |
| partial              | import from '@angriff36/manifest/projections/graphql'                                                       | mintlify/projections/graphql.mdx                | Implementation is real: src/manifest/projections/graphql/generator.ts defines GraphQLProjection (surfaces 'graphql.schema'/'graphql.resolvers'), with types.ts and index.ts. It is registered in src/manifest/projections/builtins.ts:22 (import) and :77 (registerProjection(new GraphQLProjection())), aut… |
| partial              | Kysely surface named 'kysely.schema'                                                                        | mintlify/projections/additional-projections.mdx | The Kysely projection itself is real and registered under the name 'kysely' (src/manifest/projections/kysely/generator.ts:249-253 `readonly name = 'kysely'`; registered in src/manifest/projections/builtins.ts:119 and :167), but the surface 'kysely.schema' documented at mintlify/projections/additiona… |
| partial              | Terraform surface 'terraform.main' and option 'outputMode: multi-file'                                      | mintlify/projections/additional-projections.mdx | Doc claims (C:/projects/manifest/mintlify/projections/additional-projections.mdx:37 and :48-52): surface `terraform.main` and option `outputMode: 'multi-file'`. Neither identifier exists anywhere in the implementation — a repo-wide grep for `terraform.main` and `outputMode` matches ONLY the doc file… |
| partial              | OpenAPI option 'generatedAt' for deterministic output                                                       | mintlify/projections/openapi.mdx                | The option exists only as a dead type field. mintlify/projections/openapi.mdx:54 (and the Note at :59) claims "`generatedAt` accepts an ISO timestamp override for deterministic output in tests." src/manifest/projections/openapi/types.ts:80 declares `generatedAt?: string` ("ISO timestamp override for… |
| partial              | Remix options basePath / errorBoundaries / unauthorizedRedirect / includeTenantContext / publicReads        | mintlify/projections/remix.mdx                  | None of the five documented option names exists in the Remix projection. RemixProjectionOptions (src/manifest/projections/interface.ts:324-346) and the generator's NormalizedRemixOptions/REMIX_DEFAULTS/normalizeOptions (src/manifest/projections/remix/generator.ts:50-119) contain: authProvider, authI… |
| partial              | Built-in storage targets table listing dynamodb, redis, turso, event-sourced as first-party runtime targets | mintlify/adapters/custom-stores.mdx             | Doc claim: mintlify/adapters/custom-stores.mdx:131-144 — table titled "Built-in storage targets" / "The runtime ships with first-party support for the following targets" lists dynamodb, redis, turso, event-sourced. Code on main: (1) dynamodb/redis/turso have real, tested, shipped adapter classes — D… |
| partial              | import { DynamoDBOutboxStore } from '@angriff36/manifest/outbox/dynamodb'                                   | mintlify/adapters/dynamodb.mdx                  | Implementation EXISTS but the documented import path does NOT resolve, and the doc's usage example doesn't match the real API. (1) Class is real: `export class DynamoDBOutboxStore implements OutboxStore` at C:/projects/manifest/src/manifest/outbox/stores/dynamodb.ts:100, with full enqueue/claim/mark… |
| actually-implemented | Schedules 'not yet implemented in the lexer, parser, or IR schema'                                          | docs/features/scheduled-commands.md             | The doc's "Status: Planned... not yet implemented in the lexer, parser, or IR schema... schedule lines are currently skipped during parse" banner (docs/features/scheduled-commands.md:3, restated at :72-77 "PLANNED — not in ir-v1.schema.json today") is inverse-stale: schedules are fully implemented o…

**Update (2026-07-14):** the rows above documenting `mintlify/quickstart.mdx` (policy syntax, `guard ... is not empty`, `RuntimeEngine` `userId` context, `runtime.on(...)`), `mintlify/language/manifest-files.mdx` and `mintlify/integration/nextjs.mdx`'s `command publish()`, and `mintlify/installation.mdx`'s stale version are all **FIXED-SINCE** — corrected same-day 2026-07-01 (commits `7cb8ea9`/`a2494d5`), verified directly against current file content. `docs/features/entity-inheritance.md` and `docs/features/scheduled-commands.md` no longer say "Planned" — both now correctly state their features are implemented (verified: `entity-inheritance.md:5`, `scheduled-commands.md:3`). Still **STILL-OPEN**: the "entity-first `runCommand(Entity, cmd, args)`" phantom (row above, `mintlify/language/commands.mdx`) — confirmed still present at `commands.mdx:221`, `events.mdx:83`, `approvals.mdx:133` (and likely the other pages the original audit named but this pass did not resample) — it survived the fix wave because the CI doctest gate added 2026-07-01 only compiles ` ```manifest ` blocks, not TypeScript. The mixin-syntax phantom (`mintlify/language/advanced-entities.mdx`) is **FIXED-SINCE** — the doc now shows `mixin` declared inside the entity header, matching real grammar. |

## Appendix B — Glue-gap classification (40 deduped items; tier + effort verified)

| #   | Item                                                                                                                                             | Tier           | Effort | Verified                     | What/why                                                                                                                                                                                                                                                                                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ------ | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Runtime factory module (createManifestRuntime) is never emitted                                                                                  | native-gap     | L      | confirmed                    | Every generated write path across six projections (Next.js dispatcher/routes/SSE/cron, Express, Hono, Remix, SvelteKit) imports createManifestRuntime from a module the user must hand-author; the externalExecutor mode's @/lib/manifest-executor is the same gap in another shape. The building blocks already exist unwired: createStoreProvider/config fac… |
| 2   | Next.js companion modules (@/lib/manifest-response, @/lib/database) never emitted                                                                | native-gap     | S      | confirmed                    | Generated routes import manifestSuccessResponse/manifestErrorResponse/normalizeCommandResult from responseImportPath and { database } from databaseImportPath, but no surface or CLI command emits either module; the response helper defines the entire HTTP envelope contract that ts.client and react-query hooks then assume. Fix: emit both as static com… |
| 3   | Auth companion modules never generated for any framework                                                                                         | native-gap     | M      | confirmed                    | Generated routes import auth artifacts nothing emits: Next.js @/lib/auth (authProvider 'custom') plus manual clerkMiddleware wiring, Hono ./middleware/auth requireAuth, Remix ~/utils/auth.server + session.server, SvelteKit $lib/server/auth, Express ./middleware/auth; react-query's fetchAdapter module for authenticated fetch is likewise hand-written… |
| 4   | Tenant lookup helper + assumed userTenantMapping table                                                                                           | native-gap     | M      | confirmed                    | With includeTenantFilter on, generated code either queries database.userTenantMapping.findUnique (assuming that exact table exists in the app schema) or calls a tenantProvider function whose import path is configurable but whose body is never generated (default points at '@/app/lib/tenant'.getTenantIdForOrg, a module nothing emits).                  |
| 5   | Schedules never fire: placeholder cron evaluator, hardcoded route dir, vercel.json as comment                                                    | native-gap     | M      | confirmed                    | shouldRunCron is a documented placeholder that always returns false and no timer loop exists, so the runtime never executes schedules; the nextjs.schedule surface emits routes under a hardcoded 'app/api/cron/' ignoring appDir, and the required vercel.json cron registration is emitted only as a comment inside each file. Fix: real cron evaluation (or… |
| 6   | Async commands: no durable JobQueue and no drain worker ships                                                                                    | native-gap     | M      | confirmed                    | async commands enqueue a JobRecord and hard-error without RuntimeOptions.jobQueue; the only implementation is the in-process MemoryJobQueue, and nothing drains jobs in production — users hand-implement a durable queue plus a worker calling drainJobs(). Fix: ship a PostgresJobQueue adapter and a worker helper (or `manifest jobs drain` CLI loop).      |
| 7   | Outbox delivery worker never shipped                                                                                                             | native-gap     | M      | confirmed                    | The runtime natively enqueues outbox entries, but claim/markDelivered/markFailed are never called by any shipped worker, CLI command, or loop — the docs instruct users to hand-write the ~30-line poll/claim/deliver/mark loop. Fix: ship a runOutboxWorker(store, deliver, opts) helper and/or a `manifest outbox drain` command.                             |
| 8   | Webhook declarations produce no HTTP surface outside Convex                                                                                      | native-gap     | M      | confirmed                    | webhook declarations (path, method, HMAC signature, idempotencyHeader, transform) compile to IR data only; the runtime has zero webhook code and no Next.js/Express/Hono surface generates webhook routes — users hand-mount endpoints, hand-verify signatures, and hand-apply transforms. Only convex/http.ts materializes them.                               |
| 9   | No engine transaction boundary: outbox enqueue non-atomic, no multi-command transaction                                                          | native-gap     | L      | confirmed                    | Outbox enqueue happens after mutation, outside any transaction, with failures only console.warn'd ('NOT transactional w.r.t. mutation today' — automatic threading 'deferred to a future release'); there is also no multi-command transaction, so batching must be hand-modeled as coarse commands. Fix: engine-managed transaction threading through store w… |
| 10  | Realtime is single-instance only; Redis pub/sub bridge is a stub                                                                                 | native-gap     | L      | confirmed                    | engine.subscribe is an in-process listener array and the generated SSE route depends on a module-scoped singleton engine, so serverless/multi-instance deployments silently drop events; RedisStore.subscribe is itself an in-process Map with a comment saying a real implementation needs a dedicated subscriber connection. Fix: finish the Redis pub/sub b… |
| 11  | IdempotencyStore has zero shipped implementations                                                                                                | native-gap     | S      | confirmed                    | Exactly-once command semantics require an IdempotencyStore, but the package ships only the interface — not even a memory implementation — so any dedup use requires hand-rolling get/set storage plus threading idempotencyKey per call.                                                                                                                        |
| 12  | Identity/context construction and stripping is per-handler boilerplate; config resolveUser unwired                                               | native-gap     | S      | confirmed                    | The runtime correctly refuses to auto-populate user/tenant context (spec fail-closed), but the package ships no helper to build a safe RuntimeContext: embedded users copy a documented destructure-and-strip snippet (id/userId/tenantId/orgId/user) into every handler, and the manifest.config resolveUser key is only read by `manifest scan`, never injec… |
| 13  | Approval expiry needs a user-hosted cron sweep                                                                                                   | native-gap     | S      | confirmed                    | In durable mode, pending approvals only expire if something external periodically calls approvalStore.expire(now)/expireApprovals(); no sweeper ships. Fix: emit an expiry invocation into the generated cron surface (or fold into the schedule runner once it exists) — the PostgresApprovalStore itself already ships and is a solved config option.         |
| 14  | Postgres adapter SQL schemas applied by hand from node_modules                                                                                   | native-gap     | S      | confirmed                    | The outbox, audit (and approval) Postgres adapters require their .sql schema files to be run manually against the database from inside node_modules paths. Fix: a `manifest db init` (or migrate integration) that applies the shipped adapter schemas idempotently.                                                                                            |
| 15  | Implemented adapters unreachable via package exports (outbox redis/dynamodb/mongodb, federation)                                                 | native-gap     | S      | confirmed                    | Fully implemented OutboxStore adapters for Redis, DynamoDB, and MongoDB exist in source but the exports map only exposes ./outbox/memory and ./outbox/postgres; the federation module likewise advertises '@angriff36/manifest/federation' but no such subpath exists — the redis doc even tells users to reimplement the outbox themselves. Fix: add the expo… |
| 16  | onDelete/onUpdate referential actions are inert in the reference runtime                                                                         | native-gap     | M      | confirmed                    | cascade/restrict/setNull/setDefault compile to IR and project into Prisma FK constraints, but deleteInstance performs a plain store.delete with no cascade logic — with memory/localStorage stores the declared semantics silently do not happen. Fix: implement referential actions in the engine so IR semantics hold on every store.                         |
| 17  | encrypted properties silently stored in plaintext without a provider                                                                             | native-gap     | S      | confirmed                    | Without RuntimeOptions.encryptionProvider, encrypt/decrypt around store I/O is a documented no-op — `encrypted` fields hit the store in plaintext with no error. Fix: ship a default AES-GCM provider (env-keyed) and make an encrypted property without any provider a loud construction-time error, keeping the provider option for KMS integration.          |
| 18  | flag() builtin needs static flag values in config                                                                                                | config-option  | S      | confirmed                    | flag() always evaluates to false unless the host injects RuntimeOptions.flagProvider; there is no way to declare static/default flag values in manifest.config or RuntimeOptions for the common no-vendor case. Fix: a flags map option resolved before the provider (vendor SDKs remain a one-line provider closure).                                          |
| 19  | ts.client: read-only, hardcoded '/api' prefix, naive plural envelope, no command callers                                                         | native-gap     | M      | confirmed                    | The plain TypeScript client emits only get<Entity>s/get<Entity> read functions with a hardcoded `/api/` prefix and `data.<camelEntity>s` envelope key — no POST/dispatcher command callers exist, and unlike react-query there is no apiBasePath/readEnvelope override, so non-React command invocation (server actions, scripts, tests) is entirely hand-writ… |
| 20  | No single source of truth for cross-projection contracts (route layout, validation schema names)                                                 | native-gap     | M      | confirmed                    | Three generators derive URL layout independently — nextjs (routeSegments/routeCasing/dispatcher.path), react-query (entityRoutes/dispatcherBasePath, differently shaped), and routes.manifest (which still emits the deprecated concrete `${basePath}/${entity}/${kebabCommand}` command paths with no dispatcher/routeSegments options at all) — and hono/exp… |
| 21  | Dead projection options: optimisticUpdates, hono basePath, kysely columnMappings, pydantic client options, analytics importPath                  | native-gap     | M      | confirmed                    | Five advertised options are normalized but never consulted: react-query optimisticUpdates (header even claims the feature; mutations emit only onSuccess invalidation), hono basePath (never applied to route strings), kysely columnMappings (DB columns must literally match IR names), pydantic clientBaseUrl/clientClassName (localhost:3000 hardcoded; ba… |
| 22  | Analytics projection emits non-compiling, never-invoked code                                                                                     | native-gap     | M      | confirmed                    | Generated analytics.events.ts calls provider functions (analytics.track/mixpanel.track/trackSelfDescribingEvent) without importing or initializing any SDK; per-entity handlers import <Entity>AnalyticsContext from './analytics.types', a module no surface generates; and nothing hooks track<Command>() into the runtime — users hand-write the types modu… |
| 23  | Prisma: hasMany-through emits no relation (join entity hand-modeled)                                                                             | native-gap     | M      | confirmed                    | A `hasMany X through Y` relationship emits only a comment plus a PRISMA_RELATION_VIA_THROUGH_UNIMPLEMENTED info diagnostic; users must hand-declare the join entity with two belongsTo relations in .manifest source. Fix: synthesize the join model (or the Prisma implicit m-n fields) from the through declaration.                                          |
| 24  | RLS tenancy policies emitted only as SQL comments                                                                                                | native-gap     | S      | confirmed                    | With a tenant declared, the Prisma projection adds the column and index but emits the ALTER TABLE/CREATE POLICY statements as comments ('apply manually or via migration') — enabling RLS is a hand-written migration step. Fix: emit a real .sql migration artifact as an additional surface.                                                                  |
| 25  | manifest migrate detects but never applies; drizzle surface lacks config/client                                                                  | native-gap     | M      | confirmed                    | migrate prints 'Drizzle Kit integration not yet implemented' and '(Prisma migrate invocation not implemented in this version)' then lists manual next steps — users run prisma generate/migrate dev by hand after every codegen; relatedly the drizzle projection emits only schema.ts, leaving drizzle.config.ts and the db client (both derivable) hand-writ… |
| 26  | Dart client targets routes no backend projection emits                                                                                           | native-gap     | M      | unverified (classifier only) | The generated dio client calls pluralized `/api/<snake_case(entity)>s/...` REST paths while the Express projection serves unpluralized `/<entity>/list` segments and Next.js uses its own layout — no in-repo backend matches, so users hand-write a compatible API layer or proxy rewrites. (pubspec emission already exists behind emitPackageFiles:true.)    |
| 27  | GraphQL projection is scaffolding, not a server                                                                                                  | native-gap     | L      | unverified (classifier only) | The user must hand-author '@/lib/manifest-runtime' and a Prisma-shaped '@/lib/database', implement the @auth directive (only its SDL declaration is emitted), and wire a PubSub that actually publishes — generated subscription resolvers subscribe to channels nothing ever publishes on, and mutation resolvers are stubs 'you complete against your runtim… |
| 28  | Health projection reports 'healthy' from TODO placeholders                                                                                       | native-gap     | M      | unverified (classifier only) | The generated handler contains TODO stubs for store connectivity ('// Example: await pool.query('SELECT 1')'), outbox depth (placeholder 0), and IR-hash verification — every check passes as shipped, and users must edit a file marked 'Do not edit manually'. Fix: generate real checks against the configured stores/outbox (the IR knows the store target… |
| 29  | Materialized views not derived from IR: expression-to-sql translator unwired                                                                     | native-gap     | M      | unverified (classifier only) | The projection validates entity names but every view column is a raw SQL string the user authors in projection options, plus manual DDL application and pg_cron setup — while a working IRExpression→SQL translator (expression-to-sql.ts) sits in the same directory, imported only by the test file. Fix: wire the translator so computed properties become … |
| 30  | Mongoose projection is two files and no registration                                                                                             | native-gap     | L      | unverified (classifier only) | Only options.ts and type-mapping.ts exist — no generator, no ProjectionTarget, absent from builtins.ts — so 'use resolveMongooseType as building blocks for a custom registerProjection target' is the current story. Fix: finish and register the generator, or delete the directory.                                                                          |
| 31  | CLI surface gaps: build lacks --all; five commands implemented but unregistered; no IR-dump or git-ref diff mode                                 | native-gap     | M      | unverified (classifier only) | `manifest build` (index.ts:219-248, verified) has no --all so CI hand-chains compile --all && generate --all; analyze/seed/profile/pack-unpack/generate-from-prompt are complete command modules never imported in index.ts (verified); there is no `compile --print` stdout mode (root debris compile-dynamodb-temp.mjs/debug-compile-64.mjs/dump-ir.mts exis… |
| 32  | Config plumbing: single-run generate/build resolve only nextjs options; global naming never applied; no merge-entry key                          | native-gap     | M      | unverified (classifier only) | generate -p prisma (etc.) and build feed the projection nextjs-resolved options (index.ts:198,233 verified) — only the --all path passes each projection's own config block (generate.ts:230-375 verified); the documented top-level `naming` key is typed and validated but resolveProjectionOptions has zero CLI callers (verified), so conventions must be … |
| 33  | Config schema is a closed set of 4 projections and omits real options                                                                            | native-gap     | M      | unverified (classifier only) | docs/spec/config/manifest.config.schema.json allows only nextjs/routes/prisma/prisma-store under additionalProperties:false (verified) while 27 projections are registered, and even consumed nextjs options routeCasing/dateSerialization are absent (verified) — so configuring any other projection or those options forces users to choose between the fea… |
| 34  | Config command wiring: no validate-on-generate, inspect covers 2 projections, lint-routes ignores ts/js configs, registries need manual chaining | config-option  | M      | unverified (classifier only) | compile/generate/watch never run schema validation so typo'd keys are silently dropped; `config inspect` hardcodes {nextjs, routes} effective-config only; loadLintRoutesConfig hard-codes manifest.config.yaml/.yml (verified) while the shared loader supports ts > js > yaml; and enforce-surface/audit-governance require pre-emitted registry paths nothi… |
| 35  | Plugin system is inert: loadPlugins has zero call sites                                                                                          | native-gap     | M      | unverified (classifier only) | plugins: declarations are schema-valid and `manifest plugins list` echoes them, but no compile/generate/runtime path ever calls loadPlugins (verified: only the definition and its own doc comment), so plugin projections, store adapters, builtins, audit sinks, and CLI commands never activate from config — users hand-write a bootstrap that threads Loa… |
| 36  | WASM evaluator has no artifact and no Node loader                                                                                                | native-gap     | L      | unverified (classifier only) | The WASM path expects a manifest-runtime.wasm that exists nowhere in the repo (verified: src/manifest/wasm/ contains only .ts files) and the loader is fetch-only with Node loading unimplemented — users wanting WASM speed must compile and host the module themselves, so the TS evaluator is the de facto behavior. Fix: build and ship the artifact with … |
| 37  | Framework shells and infra deploy pipelines are the app's job                                                                                    | template-ok    | S      | unverified (classifier only) | Owning the framework/infra project is genuinely app-specific: mounting the Express/Fastify/Hono app (wrangler.toml, CORS, error middleware), placing Remix/SvelteKit artifacts into an existing scaffold with tsconfig aliases and deps, running terraform apply / cdk deploy / drizzle-kit / convex dev, PUTting Elasticsearch mappings and running its index… |
| 38  | Host event wiring to vendor infrastructure (onEvent handlers)                                                                                    | template-ok    | S      | unverified (classifier only) | Bridging emitted events to Ably/BullMQ/Sentry/webhook-retry pipelines, persisting event-sourced history, wiring a dedicated Redis subscriber connection, and custom AuditSink backends (Datadog/OTel) are hand-written runtime.onEvent/AuditSink code per app — docs/guides/event-wiring.md is a ~630-line catalog of it. Fix: keep as documented templates; t… |
| 39  | Stale docs prescribing glue the code no longer requires                                                                                          | already-solved | S      | unverified (classifier only) | Three doc claims are already solved on main: (1) 'call the projection functions directly in your build scripts' — generate --all/watch --all/compile --all are native and config-driven (and the shown generateRoute/generateTypes API doesn't exist); (2) custom-stores.mdx has users hand-write a ~50-line PrismaStore while ./stores/prisma-generic ships i… |
| 40  | Distribution and tooling debt: stdlib unpublished, VSIX manual, dead schema URLs, duplicate LSP, watch fallback                                  | native-gap     | M      | unverified (classifier only) | @manifest/stdlib documents a node_modules consumption path but is never published and its .manifest files aren't in the root files allowlist; the VS Code extension must be built and installed from a .vsix by hand; the config schema $id (manifest.lang) is a dead URL requiring hand-mapped .vscode/settings.json (SchemaStore PR was in flight); two para… |

## Appendix C — Full coverage matrix (143 rows)

| Feature                                                                                                                                              | Mintlify       | Internal docs                   | Notes                                                                                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Language: entity declaration (+external modifier, key/unique, timestamps, realtime, store-in, policies refs)                                         | full           | full                            | parser.ts:150-340, ir.ts:126-160. mintlify entities.mdx + manifest-files.mdx; internal spec + features pages.                                                                                                                                               |
| Language: module declarations + use imports (multi-file compilation, cycle detection, topo sort)                                                     | full           | full                            | parser.ts:95-148, module-resolver.ts, multi-compiler.ts. modules.mdx verified --merge/--entry; internal modules & use imports feature page. Namespace-disambiguation syntax claim in modules.mdx is unverifiable as written.                                |
| Language: enum declarations (labels, ordinals)                                                                                                       | full           | full                            | parser.ts:342-385. Neither doc set flags that the runtime does NOT validate enum membership (zero enum handling in runtime-engine.ts).                                                                                                                      |
| Language: value objects                                                                                                                              | full           | full                            | parser.ts:387-418. Code is partial (no runtime embedding/validation); types.mdx and internal features cover the syntax.                                                                                                                                     |
| Language: property modifiers (required/unique/indexed/private/readonly/optional/searchable, defaults)                                                | full           | full                            | parser.ts:436-484. entities.mdx covers all; unique/indexed/required are projection concerns not runtime-enforced — under-flagged in both.                                                                                                                   |
| Language: autoNow property defaults (= now() / = today())                                                                                            | full           | full                            | ir-compiler.ts:778-799, runtime-engine.ts:1924-1926. timestamps.mdx + docs/features/date-time-types.md accurate. today() is default-only, not a runtime builtin.                                                                                            |
| Language: computed/derived properties + cache request/session/ttl                                                                                    | full           | full                            | parser.ts:655-690, runtime-engine.ts:4119-4218. computed-properties.mdx says 'no caching layer in the default runtime' contradicting computed-caching.mdx on the adjacent nav page.                                                                         |
| Language: relationships (hasMany/hasOne/belongsTo/ref, through, fields/references, onDelete/onUpdate)                                                | full           | partial                         | parser.ts:767-820. entities.mdx covers referential actions (v1.0 wave); internal docs/projections/prisma.md predates the wave. Neither flags that the runtime never executes cascades (projection-level only).                                              |
| Language: composite primary keys + alternate unique keys                                                                                             | full           | partial                         | parser.ts:289-296, ir.ts:142-144. entities.mdx covers; internal prisma.md predates composite @@unique support. Runtime still keys instances by single id — not flagged.                                                                                     |
| Language: optimistic concurrency (versionProperty/versionAtProperty)                                                                                 | full           | full                            | runtime-engine.ts:1942-1947, 2087-2113. entities.mdx + faq + docs/reference/runtime-engine.md.                                                                                                                                                              |
| Language: timestamps entity modifier (injected createdAt/updatedAt)                                                                                  | full           | full                            | parser.ts:299-302, ir-compiler.ts:665-674. timestamps.mdx accurate.                                                                                                                                                                                         |
| Language: realtime entity flag (projection hint)                                                                                                     | full           | full                            | parser.ts:303-310, ir.ts:151-152. realtime-subscriptions pages in both sets correctly treat it as an SSE projection hint.                                                                                                                                   |
| Language: state transitions (transition prop from/to)                                                                                                | full           | full                            | parser.ts:520-543, runtime-engine.ts:2129-2134. entities.mdx; internal guides/primitives-reference.md + spec/semantics.md.                                                                                                                                  |
| Language: entity inheritance (extends) + mixin composition                                                                                           | partial        | partial                         | Implemented (parser.ts:162-179, entity-composition.ts). mintlify advanced-entities.mdx documents WRONG mixin syntax (top-level mixin declaration; real grammar puts mixin in the entity header). Internal docs/features/entity-inheritance.md falsely says… |
| Language: generic entities (entity X<T>)                                                                                                             | missing        | partial                         | NOT implemented — fixture 84 pins the parse error 'Expected {, got <'. Internal entity-inheritance.md discusses generics as part of the feature → phantom.                                                                                                  |
| Language: commands (params, optional, defaults, returns, => shorthand)                                                                               | full           | full                            | parser.ts:822-895, runtime-engine.ts:2225+. commands.mdx thorough but its runCommand examples use the wrong entity-first signature.                                                                                                                         |
| Language: guards (guard/when, strict in-order first-falsey halt)                                                                                     | full           | full                            | parser.ts:861. guards-policies.mdx + internal guides.                                                                                                                                                                                                       |
| Language: constraints (severity ok/warn/block, code, messageTemplate, details, entity+command level)                                                 | full           | full                            | parser.ts:1412-1589, runtime-engine.ts:1878-2045. entities.mdx accurate; constraints.mdx's own min(self.quantity,0) example is semantically broken (numeric result 0 is falsey for valid data).                                                             |
| Language: constraint overrides (overrideable, overridePolicy, OverrideRequest)                                                                       | full           | full                            | ir.ts:554-563, runtime-engine.ts:2231; fixtures 22/52/53.                                                                                                                                                                                                   |
| Language: policies (read/write/delete/execute/all/override), default policies, policy rateLimit                                                      | partial        | full                            | parser.ts:897-916, 239-247. guards-policies.mdx documents a phantom command-body policy clause — verified: parseCommand (parser.ts:848-876) accepts only retry/rateLimit/guard/when/constraint/emit/actions; manifest-files.mdx and roles.mdx correctly de… |
| Language: roles + hierarchy (extends, deny-absolute, hasPermission/roleAllows)                                                                       | full           | full                            | parser.ts:918-946, ir-compiler.ts:1391-1448. roles.mdx + docs/features role hierarchy page.                                                                                                                                                                 |
| Language: actions mutate/compute/emit/publish/persist/effect                                                                                         | partial        | partial                         | parser.ts:1393-1410, runtime-engine.ts:3757-3810. Code partial: persist/effect only evaluate + trace (stubs); deterministic mode hard-errors on them. NEITHER doc set flags stub status; faq.mdx action list omits mutate/emit.                             |
| Language: events (declarations, emit with brace payloads, event log, correlation/causation)                                                          | full           | full                            | parser.ts:970-994, 863-872; runtime-engine.ts:3200-3260. events.mdx thorough but carries event-sourced-store and runtime-webhook phantoms plus wrong runCommand signatures.                                                                                 |
| Language: reactions (on/run/resolve/params) + fanOut 1:N cascades + depth limit                                                                      | full           | full                            | parser.ts:1105-1144, runtime-engine.ts:3262-3330. reactions.mdx + docs/features/event-reactions.md both current through the v2.14 wave.                                                                                                                     |
| Language: aggregate count(Entity where ...) expression                                                                                               | full           | full                            | parser.ts:1797-1841, runtime-engine.ts:3963-3985. Both note equality-only predicates.                                                                                                                                                                       |
| Language: approval workflows (stages, timeout, on_timeout, runtime approve/deny API, durable store)                                                  | full           | full                            | parser.ts:545-653, runtime-engine.ts:4721-4893. approvals.mdx examples call async requestApproval/approveStage without await; internal approval-workflows.md accurate.                                                                                      |
| Language: sagas (steps, compensate, on_failure)                                                                                                      | full           | full                            | parser.ts:1313-1391, runtime-engine.ts:2393-2510. workflows.mdx + saga-workflow.md.                                                                                                                                                                         |
| Language: async commands + synthesized Completed/Failed events + JobQueue/drainJobs                                                                  | full           | full                            | ir-compiler.ts:458-504, runtime-engine.ts:2283-2324. Both accurate; mintlify examples use wrong runCommand signature.                                                                                                                                       |
| Language: command retry policy (maxAttempts/backoff/delay/jitter/retryOn)                                                                            | full           | full                            | parser.ts:1883-1938, runtime-retry.ts. commands.mdx + security-features.md.                                                                                                                                                                                 |
| Language: rate limiting (command + policy scoped)                                                                                                    | full           | full                            | parser.ts:2028-2071, runtime-rate-limit.ts. Neither flags that limits are per-engine in-memory (multi-instance gets per-instance limits, runtime-engine.ts:796).                                                                                            |
| Language: schedules (cron/interval/every + runSchedule)                                                                                              | partial        | partial                         | ~~Code partial: shouldRunCron is a placeholder returning false (runtime-schedule.ts:40-45), no ticker ships.~~ **Update (2026-07-14): FIXED (WS2B) — `shouldRunCron` (`runtime-schedule.ts:182-184`) now delegates to a real cron-field evaluator, verified directly; no longer hardcoded `false`.** workflows.mdx places schedule inside entity bodies (parser only allows top/module level). Internal scheduled-commands.md falsely claims the fea… |
| Language: webhooks (declaration, hmac signature config, transform)                                                                                   | partial        | partial                         | Code partial: IR-only; runtime never receives/verifies HTTP (zero webhook refs in runtime-engine.ts); only Convex materializes handlers. events.mdx wrongly attributes signature verification to the runtime and shows an entity-scoped webhook. Internal … |
| Language: multi-tenancy (tenant declaration, fail-closed gate, read filtering)                                                                       | full           | full                            | parser.ts:420-434, runtime-engine.ts:2254-2264. tenancy.mdx + tenant-isolation.md both accurate.                                                                                                                                                            |
| Language: data masking (masked strategies + unmask when)                                                                                             | full           | full                            | parser.ts:446-518, runtime-engine.ts:1806-1860. entities.mdx + security-features.md.                                                                                                                                                                        |
| Language: field encryption (encrypted modifier + EncryptionProvider hook)                                                                            | full           | full                            | runtime-engine.ts:877-935. No concrete provider ships (user implements interface) — security-features.md is accurate about this.                                                                                                                            |
| Language: feature flags (flag() builtin + flagProvider)                                                                                              | full           | full                            | runtime-engine.ts:1462-1468. Both pages unusually honest and accurate.                                                                                                                                                                                      |
| Language: core type system (string/number/boolean/list/map/any, T?, generics, T[] arrays)                                                            | partial        | full                            | parser.ts:996-1024. mintlify self-contradicts: entities.mdx claims only 'string, number, boolean, timestamp'; types.mdx and manifest-files.mdx disagree with it and each other.                                                                             |
| Language: decimal(p,s)/money(p,s) types                                                                                                              | full           | full                            | Code partial (runtime arithmetic is IEEE-754; precision is projection-only). Internal decimal feature page honestly flags missing compile-time precision validation.                                                                                        |
| Language: date/time/datetime/duration types + write-time validation + duration builtins                                                              | partial        | full                            | date-time.ts, runtime-engine.ts:1989-2032. mintlify types.mdx flatly DENIES these types exist ('keywords not in the lexer') — stale-wrong; manifest-files.mdx on the same site documents them correctly. Internal date-time-types.md accurate.              |
| Language: expression operators (ternary, logical, in/contains, ?., lambdas, literals)                                                                | full           | full                            | parser.ts:1688-1751. ?. parses identically to . (no distinct optional-chaining semantics) — not flagged anywhere.                                                                                                                                           |
| Language: builtin expression function library (~50 builtins)                                                                                         | full           | full                            | runtime-engine.ts:1261-1481; docs/spec/builtins.md matches code. expressions.mdx carries the registerBuiltin phantom for custom functions.                                                                                                                  |
| Language: store declarations (memory/localStorage/postgres/supabase/mongodb/durable/custom)                                                          | full           | full                            | parser.ts:948-968, runtime-engine.ts:988-1057. stores.mdx honest that non-memory targets need adapters. 'filesystem' silently aliasing to localStorage (ir-compiler.ts:422) undocumented.                                                                   |
| Language: flow/effect/expose/compose declarations (parse-only stubs with warning)                                                                    | partial        | missing                         | parser.ts:1591-1686; ir-compiler emits 'not lowered to IR' warnings. manifest-files.mdx mentions them and links to advanced-entities.mdx which does not cover them; internal has nothing user-facing.                                                       |
| Language: behaviors (on Event { actions } on entities) — parsed then silently dropped                                                                | missing        | missing                         | parser.ts:1026-1050 parses them; IREntity has no behaviors field and ir-compiler never reads them — dropped WITHOUT a warning. Undocumented in both sets; the silent-drop is a code hazard worth documenting.                                               |
| Language: readmodel construct                                                                                                                        | missing        | missing                         | NOT implemented — fixture 86 pins 8 parse errors; no readmodel keyword in lexer. event-sourced-store.mdx correctly retracts earlier phantom queryReadModel claims. Both doc sets correctly avoid claiming it.                                               |
| Projections: registry + generic CLI dispatch (27 registered built-ins)                                                                               | partial        | partial                         | registry.ts:48-55, builtins.ts:50-133; any projection reachable via manifest generate -p <name> (generate.ts:187-233). mintlify integration/projections.mdx lists only nextjs+prisma with a phantom method API; internal docs/projections/README.md lists … |
| Projections: Next.js (10 surfaces: route/detail/command/dispatcher/subscribe/subscriptionHook/sharedRuntime/schedule/ts.types/ts.client)             | full           | full                            | nextjs/generator.ts:525. mintlify nextjs.mdx and internal docs/projections/nextjs.md both thorough; integration/projections.mdx contradicts with 'four surfaces'. ts.client is read-only (no command callers) with hardcoded /api prefix — under-documente… |
| Projections: Next.js canonical dispatcher (inline/externalExecutor, deriveInstanceId)                                                                | full           | full                            | generator.ts:1037-1193; configuration.mdx covers executionMode migration. Code's own defaults.ts comment/value drift on deriveInstanceId (says off, is true) unflagged.                                                                                     |
| Projections: Next.js realtime SSE (subscribe/subscriptionHook/sharedRuntime)                                                                         | full           | full                            | generator.ts:1200-1440. Single-instance-server limitation documented in both.                                                                                                                                                                               |
| Projections: nextjs.schedule cron routes                                                                                                             | partial        | partial                         | schedule-generator.ts hardcodes app/api/cron path ignoring appDir; ~~vercel.json entry is a comment only;~~ **Update (2026-07-14): FIXED (WS2B) — vercel.json is now emitted as strict JSON, not a comment (schedule-generator.ts:138); nextjs.schedule is also CLI-reachable via generate.ts:400-403.** CRON_SECRET auth skipped when env unset. workflows.mdx links to a non-existent Next.js cron page; internal nextjs.md thin here.                     |
| Projections: routes projection (routes.manifest + routes.ts)                                                                                         | partial        | partial                         | routes/generator.ts:426-476. Covered only via config schema (RoutesProjectionOptions) and the separate `manifest routes` CLI mention; no projection page in either set. Code gaps (no routeCasing support, deprecated concrete-route layout, dead $schema … |
| Projections: Prisma (multiSchema, naming convention, autoBackRelations, referential actions, composite keys, prisma.config.ts companion)             | full           | partial                         | prisma/generator.ts:1124-1284. mintlify integration/prisma.mdx is the most thorough page; internal docs/projections/prisma.md predates the 2.20-2.22 wave (no multiSchema/naming/autoBackRelations). 'through' M:N unimplemented — flagged via diagnostic,… |
| Projections: prisma-store (metadata + registry surfaces, GenericPrismaStore bridge)                                                                  | partial        | partial                         | prisma-store/generator.ts:14-64. Config options documented (configuration.mdx + schema PrismaStoreProjectionOptions) but neither set has a projection page explaining the metadata/registry surfaces or the runtime store it feeds.                         |
| Projections: Convex (schema/queries/mutations/crons/http/sagas, policyMode, referenceMode)                                                           | full           | missing                         | convex/generator.ts:49-55; most mature projection. mintlify convex.mdx accurate; absent from internal docs/projections/README.md and no internal page.                                                                                                      |
| Projections: React Query (hooks + provider, entityRoutes, readEnvelope)                                                                              | full           | full                            | react-query/generator.ts:555-609. Both pages honestly flag inert optimisticUpdates/typesImportPath; both carry the stale 'Next.js only CLI' claim. Hardcoded pathHints and non-configurable /list and /:id suffixes under-documented.                       |
| Projections: Drizzle (drizzle.schema, 3 dialects)                                                                                                    | full           | full                            | drizzle/generator.ts (51 tests). Both accurate.                                                                                                                                                                                                             |
| Projections: Express/Fastify (router/entity/types/all)                                                                                               | full           | full                            | express/generator.ts:40-45. Both carry the stale 'no dedicated CLI command' claim. Zero unit tests (snapshot-only) and Dart-client path mismatch unflagged.                                                                                                 |
| Projections: Hono (router/entity/types/all)                                                                                                          | full           | full                            | hono/generator.ts:594. basePath is a DEAD option (normalized, never used in route emission) yet listed in both docs → phantom. Stale CLI claim in both.                                                                                                     |
| Projections: GraphQL (SDL + resolver stubs)                                                                                                          | full           | full                            | graphql/generator.ts (41 tests). mintlify claims a '@angriff36/manifest/projections/graphql' subpath that is not in the exports map. @auth directive is declaration-only — thinly flagged.                                                                  |
| Projections: JSON Schema (entity/schemas, 3 drafts)                                                                                                  | full           | full                            | jsonschema/generator.ts:380. Stale CLI claim in both.                                                                                                                                                                                                       |
| Projections: LLM-context (full/summary/ir)                                                                                                           | full           | full                            | llm-context/generator.ts:347. Both honest about inert emitHeader and nondeterministic buildMeta timestamp.                                                                                                                                                  |
| Projections: Mermaid (er/state/sequence) + manifest diagram CLI                                                                                      | full           | full                            | mermaid/generator.ts:431. mermaid.mdx CLI flags exactly match index.ts:429-444. Every required property labeled PK in ER output — unflagged oddity.                                                                                                         |
| Projections: OpenAPI (openapi.spec, 3.1.0)                                                                                                           | full           | full                            | openapi/generator.ts. generatedAt option is declared in types.ts:80 but never referenced in the generator (dead) — documented in mintlify openapi.mdx → borderline phantom.                                                                                 |
| Projections: Zod (entity/command/schemas)                                                                                                            | full           | full                            | zod/generator.ts:355-357. Cleanest options hygiene in code; docs accurate.                                                                                                                                                                                  |
| Projections: Kysely (typed Database interfaces + factory)                                                                                            | partial        | missing                         | kysely/generator.ts:249. mintlify additional-projections.mdx is a summary with a WRONG surface name (kysely.schema; real is kysely.types — verified generator.ts:50) and lists dead columnMappings option. No internal page.                                |
| Projections: Terraform (aws/gcp/supabase infra)                                                                                                      | partial        | missing                         | terraform/generator.ts:824. additional-projections.mdx claims surface 'terraform.main' and option 'outputMode' — real surface is terraform.hcl (generator.ts:45) and real option emitSingleFile (verified). No internal page.                               |
| Projections: Materialized views (DDL)                                                                                                                | partial        | missing                         | Code partial: expression-to-sql translator exists but is NOT wired (columns are raw-SQL passthrough); trigger debounce emits a comment only. Summary-only mintlify coverage; no internal page.                                                              |
| Projections: Analytics (tracking-plan/events/handlers)                                                                                               | partial        | missing                         | analytics/generator.ts:38-45. importPath option is dead (client never imported) and output embeds new Date() — neither flagged in the summary-only mintlify section. No internal page.                                                                      |
| Projections: Elasticsearch (mapping/indexTemplate/ingestPipeline/indexer/client)                                                                     | partial        | missing                         | elasticsearch/generator.ts:55-66. Summary-only mintlify coverage. Code's own header claims readModels discovery that never happens. No internal page.                                                                                                       |
| Projections: Storybook (CSF3 stories, guard/constraint scenarios)                                                                                    | partial        | missing                         | storybook/generator.ts:427-429. Summary-only mintlify; no internal page.                                                                                                                                                                                    |
| Projections: Health check (handler/nextjs/express)                                                                                                   | missing        | missing                         | IMPLEMENTED (health/generator.ts:358, 39 tests, registered builtins.ts:98) but documented NOWHERE in either set. Generated code is scaffolding (TODO store checks, placeholder outbox depth) — would need honest framing.                                   |
| Projections: Remix (list/detail/command/types/client)                                                                                                | full           | missing                         | remix/generator.ts:726, registered builtins.ts:113 but NOT re-exported from projections/index.ts. remix.mdx option list uses wrong names (basePath/errorBoundaries/unauthorizedRedirect/includeTenantContext/publicReads — none exist in interface.ts:324-… |
| Projections: SvelteKit (server/load/command/types/client)                                                                                            | full           | missing                         | sveltekit/generator.ts:1085 (40 tests). sveltekit.mdx lists options basePath/dispatcherImportPath/includeTenantContext/publicReads which do not exist in sveltekit/types.ts (grep-verified) → phantoms. No internal page.                                   |
| Projections: Pydantic (entity/command/models/client)                                                                                                 | full           | missing                         | pydantic/generator.ts:671-673. python-pydantic.mdx lists customImports (nonexistent, grep-verified across projections) and clientBaseUrl/clientClassName/emitConvenienceFunctions which are dead in code (client hardcodes ManifestClient + localhost:3000… |
| Projections: Dart/Flutter (entity/command/models/client/providers/package)                                                                           | full           | missing                         | dart/generator.ts:928-938. flutter.mdx lists nonexistent customImports option (grep-verified). Generated Dart never compiled in CI — unflagged. No internal page.                                                                                           |
| Projections: DynamoDB infra (cloudformation/cdk/terraform)                                                                                           | partial        | missing                         | Code partial: no GSIs despite header claim, PROVISIONED billing emits invalid CloudFormation, Terraform surface ignores billingMode. adapters/dynamodb.mdx covers the projection but not these gaps. No internal page.                                      |
| Projections: Mongoose (unregistered stub)                                                                                                            | missing        | missing                         | Only options.ts + type-mapping.ts exist — no generator, not registered, requesting 'mongoose' errors. Correctly undocumented in both sets.                                                                                                                  |
| Projections: snapshot/determinism test suite + coverage reporter                                                                                     | partial        | partial                         | snapshot.test.ts:258 asserts 27 built-ins. mintlify snapshot-testing.mdx says 'exactly 13' and documents a phantom CoverageReporter/@angriff36/manifest/coverage export; internal snapshot-testing.md says 'exactly 20'. Both stale.                        |
| CLI: manifest init (+ --ci github)                                                                                                                   | full           | full                            | index.ts:116-131, init-ci.ts. commands.mdx + ci-cd.mdx + cli.md.                                                                                                                                                                                            |
| CLI: manifest compile (-g/--merge/--entry/--all config-driven merged)                                                                                | full           | partial                         | index.ts:138-160, compile.ts:311-326. Internal cli.md omits the --all flag (index.ts:147). --merge/--entry verified in modules.mdx.                                                                                                                         |
| CLI: manifest generate (-p any registered projection, -s, --check drift gate, --all)                                                                 | full           | partial                         | index.ts:167-211, generate.ts:551-616. Internal cli.md has a stale -p list and omits --all. NEITHER set documents the defect that single-projection `generate -p prisma` receives projections.nextjs.options instead of prisma's (generate.ts:194-196 via … |
| CLI: manifest build + manifest watch (incl. watch --all, --debounce, --events)                                                                       | full           | partial                         | index.ts:218-290. cli.md omits watch --all. build has no --all in code — docs consistent.                                                                                                                                                                   |
| CLI: manifest validate / validate-ai / check                                                                                                         | full           | full                            | index.ts:297-303, 348-363, 470-486. commands.mdx + cli.md.                                                                                                                                                                                                  |
| CLI: manifest scan (policy coverage, store consistency)                                                                                              | full           | full                            | index.ts:498-507. governance.mdx + cli.md.                                                                                                                                                                                                                  |
| CLI: manifest doctor + runtime-check + inspect entity/duplicates/cache-status                                                                        | full           | full                            | index.ts:996-1007, 943-968, 805-818; doctor-lib.ts. governance.mdx/dev-tools.mdx + cli.md.                                                                                                                                                                  |
| CLI: manifest fmt / diagram / docs                                                                                                                   | full           | full                            | index.ts:310-323, 429-444, 409-422. docs command section in commands.mdx embeds internal mergeIrs spec-speak — style, not accuracy.                                                                                                                         |
| CLI: manifest diff (source-vs-ir                                                                                                                     | ir-vs-ir       | breaking) + migrate + changelog | full                                                                                                                                                                                                                                                        | full                                                                       | index.ts:823-899, 909-925. ci-cd.mdx classification table plausible but not row-verified. ir-version-control.mdx documents WRONG changelog flags (--old-ir/--new-ir; real is git refs) → phantom. |
| CLI: manifest versions (list/show/save/diff/changelog/tag/rollback/verify)                                                                           | partial        | full                            | index.ts:1084-1182. ir-version-control.mdx appends phantom compress/decompress subcommands and a phantom compression module; internal IR version control feature page accurate.                                                                             |
| CLI: manifest generate-tests (LLM fixture generation)                                                                                                | partial        | missing                         | index.ts:373-402, gen-tests.ts. dev-tools.mdx mentions it; ai-tooling.mdx documents phantom flags (--per-entity/--include-negative/--format conformance                                                                                                     | vitest) and wrong behavior. cli.md omits it despite claiming completeness. |
| CLI: manifest mock (local HTTP mock server)                                                                                                          | partial        | missing                         | index.ts:533-548 (port 4000, --host/--cors/--scenario). dev-tools.mdx matches code; testing.mdx contradicts it with phantom flags (port 3456, --output/--format/--seed). cli.md omits it.                                                                   |
| CLI: manifest repl                                                                                                                                   | partial        | missing                         | index.ts:978-994, repl.ts. runtime-tooling.mdx documents a wrong command table (load/create/reset don't exist; eval returns 'not yet implemented'). cli.md omits it.                                                                                        |
| CLI: manifest coverage                                                                                                                               | full           | full                            | index.ts:727-748. Flags match. Programmatic CoverageReporter claim in snapshot-testing.mdx is phantom (tracked there).                                                                                                                                      |
| CLI: manifest routes / lint-routes                                                                                                                   | full           | full                            | index.ts:558-583. Neither flags that lint-routes reads ONLY manifest.config.yaml/.yml directly (ignores .ts/.js configs, lint-routes.ts:74-95).                                                                                                             |
| CLI: governance suite (audit-governance, enforce-surface, audit-bypasses, audit-routes, integration-check, emit registries)                          | full           | full                            | index.ts:592-718, 788-798, 1017-1042. governance.mdx accurate; internal integration-check.md content correct but framed as 'v0.5-era' with GH-Packages heading — stale framing.                                                                             |
| CLI: manifest config (validate                                                                                                                       | print-defaults | inspect)                        | full                                                                                                                                                                                                                                                        | full                                                                       | index.ts:1050-1077, config.ts. Neither flags that inspect's EffectiveConfig covers only nextjs+routes projections, nor that validation is never run by compile/generate paths.                    |
| CLI: manifest preflight / harness / install-hooks / load-test / plugins list                                                                         | full           | partial                         | index.ts:451-463, 516-523, 330-341, 758-786, 1189-1227. dev-tools.mdx covers all; internal cli.md omits load-test.                                                                                                                                          |
| CLI: unregistered commands (analyze, seed, profile, pack/unpack)                                                                                     | missing        | missing                         | Fully implemented files in packages/cli/src/commands (analyze/seed tested) but never imported in index.ts — the commands do not exist at the CLI. Correctly absent from docs (grep of mintlify for 'manifest seed                                           | pack                                                                       | unpack' finds nothing); worth docum…                                                                                                                                                              |
| CLI/packaging: bin entries (manifest, manifest-mcp, manifest-lsp), jiti dev bin, published dist                                                      | full           | full                            | package.json:229-231, 289. installation.mdx matches exports/bins but says 'Current version: 2.18.0' (actual 2.22.0) and 'four main commands' (~40 registered) — stale.                                                                                      |
| Config: file discovery + precedence (ts > js > yaml > yml > .manifestrc)                                                                             | full           | full                            | utils/config.ts:189-202, 362-374. configuration.mdx + spec schema.                                                                                                                                                                                          |
| Config: defineConfig typed TS surface (stores/resolveUser/build)                                                                                     | full           | full                            | src/manifest/config.ts:225-247. Types deliberately exclude vNext sections; internal proposal doc covers the gap. createStoreProvider/createUserResolver have no CLI callers (programmatic-only) — under-flagged.                                            |
| Config: JSON-schema validation (manifest config validate)                                                                                            | full           | full                            | config-validate.ts:112-132 against bundled docs/spec/config/manifest.config.schema.json. Neither set flags that generate --all loads config RAW without validation.                                                                                         |
| Config: top-level src / output / prismaSchema / env preflight / hooks keys                                                                           | full           | full                            | schema :13-26, :57-84, :135-157; consumed by compile/generate/preflight/install-hooks. configuration.mdx + schema in sync.                                                                                                                                  |
| Config: plugins declarations                                                                                                                         | partial        | partial                         | Schema :85-104. configuration.mdx:112-123 says plugins are 'loaded by the CLI' with options 'passed at load time' — FALSE: loadPlugins (plugin-loader.ts:284) has zero CLI call sites; `plugins list` only echoes declarations. Phantom recorded.           |
| Config: global naming inheritance (top-level naming layered under projections)                                                                       | partial        | partial                         | Schema-valid and documented, but resolveProjectionOptions (src/manifest/config.ts:263-272) is only called from tests; generateAllFromConfig passes projection options raw (generate.ts:594). Documented capability with no CLI effect → phantom (borderlin… |
| Config: projections.nextjs options (incl. dispatcher, concreteCommandRoutes, readRoutes)                                                             | full           | full                            | Schema :171-372 vs defaults.ts/interface.ts. Known drift: routeCasing + dateSerialization consumed by code but REJECTED by the additionalProperties:false schema — undocumented in both.                                                                    |
| Config: projections.prisma + projections.prisma-store options                                                                                        | full           | full                            | Schema :464-796 in exact sync with options.ts; full worked example docs/spec/config/manifest.config.full-prisma.example.ts with validating test.                                                                                                            |
| Config: schema's closed projections set (only 4 of 27 validate)                                                                                      | missing        | missing                         | Schema :27-37 allows only nextjs/routes/prisma/prisma-store with additionalProperties:false while generate --all runs ANY registered projection — configuring convex/zod/etc. works but fails `config validate`. This false-negative gap is documented now… |
| Config: NamingConvention column PascalCase (code accepts, schema rejects)                                                                            | missing        | partial                         | naming.ts:18-25 accepts full CaseStyle for column; schema :810-813 restricts to snake_case                                                                                                                                                                  | camelCase                                                                  | preserve. Drift documented nowhere user-facing.                                                                                                                                                   |
| Runtime: command execution pipeline (fixed order: policies → constraints → guards → approval → actions → flush → emits → reactions → outbox → audit) | partial        | full                            | runtime-engine.ts:2225-3260. mintlify contradicts itself: runtime-middleware.mdx has the CORRECT constraints-before-guards order; faq.mdx and embedded-runtime.mdx have guards-before-constraints (wrong); integration/overview.mdx omits the constraints … |
| Runtime: runCommand(commandName, input, {entityName, instanceId}) API                                                                                | partial        | full                            | runtime-engine.ts:2225-2231. Correct in integration/overview.mdx + embedded-runtime.mdx; WRONG entity-first 'runCommand(Entity, cmd, args)' shape appears in commands/events/approvals/reactions/roles/async-commands/federation pages → phantom recorded.… |
| Runtime: batched persistence (command-scoped write buffer, atomic-on-failure)                                                                        | missing        | full                            | runtime-engine.ts:774-790, 3123-3200. Internal reference/runtime-engine.md includes it; no mintlify coverage of the atomicity guarantee.                                                                                                                    |
| Runtime: built-in Memory/LocalStorage stores + store initialization rules                                                                            | full           | full                            | runtime-engine.ts:626, 666, 988-1057. stores.mdx honest that only memory/localStorage are built-in.                                                                                                                                                         |
| Runtime/adapters: Postgres/Supabase/MongoDB store classes (stores.node.ts, injected via storeProvider)                                               | full           | full                            | stores.node.ts:39-438. custom-stores.mdx + implementing-custom-stores.md; Store interface verified matching in both.                                                                                                                                        |
| Runtime/adapters: DynamoDBStore                                                                                                                      | partial        | missing                         | stores.node.ts:485-622. adapters/dynamodb.mdx implies `store Order in dynamodb` works out of the box (it requires storeProvider/plugin — runtime-engine.ts:1044) and documents a failing DynamoDBOutboxStore import → phantoms. No internal page.           |
| Runtime/adapters: RedisStore (CRUD+TTL; subscribe stub)                                                                                              | full           | missing                         | stores.node.ts:644-757. redis.mdx honestly flags unexported outbox and local-only subscribe; omits the plugin-adapter requirement for `store X in redis`. No internal page.                                                                                 |
| Runtime/adapters: TursoStore (LibSQL)                                                                                                                | partial        | missing                         | stores.node.ts:797-908. turso.mdx has 3 verified errors: phantom schemaPrefix option, per-entity typed schema claim (real: one generic JSON table), and a phantom turso CLI projection. No internal page.                                                   |
| Runtime/adapters: GenericPrismaStore (metadata-driven durable store, ./stores/prisma-generic)                                                        | missing        | partial                         | src/manifest/stores/prisma-generic/store.ts, exported subpath. Appears only in internal config schema/example (grep verified); NO prose doc in either set mentions the shipped class — custom-stores.mdx even hand-writes a duplicate PrismaStore instead.  |
| Runtime: 'durable' + custom store targets via storeProvider/plugin schemes                                                                           | full           | full                            | runtime-engine.ts:1033-1050. stores.mdx + plugin-api.mdx accurate; 'eventSourced' fixture target is just a custom string needing an adapter.                                                                                                                |
| Runtime: event-sourced store target                                                                                                                  | partial        | missing                         | NOT implemented — zero EventSourcedStore code. mintlify is self-contradictory: event-sourced-store.mdx honestly retracts it, but events.mdx documents 'store Order in event-sourced' with append-only/snapshot/replay and custom-stores.mdx lists it as bu… |
| Adapters: outbox store (contract + Memory/Postgres, at-least-once, non-transactional enqueue)                                                        | full           | partial                         | outbox/outbox-store.ts, stores/memory.ts, stores/postgres.ts; exports verified. outbox.mdx honest; internal transactional-outbox guide predates the adapter and never mentions it (docs/README.md table does).                                              |
| Adapters: outbox DynamoDB/MongoDB/Redis stores (implemented, UNEXPORTED)                                                                             | partial        | missing                         | src/manifest/outbox/stores/{dynamodb,mongodb,redis}.ts real but no package.json subpath. redis.mdx honestly flags it; dynamodb.mdx documents the failing import → phantom. Nothing internal.                                                                |
| Adapters: audit sink (contract + Memory/Postgres, one record per runCommand)                                                                         | full           | full                            | audit/audit-sink.ts, runtime-engine.ts:2371-2381. audit-sink.mdx verified against exports. Stale 'land in a follow-on' header comments live in code, not docs.                                                                                              |
| Adapters: approval store (contract + Memory/Postgres, durable gate)                                                                                  | full           | full                            | approval/approval-store.ts, runtime-engine.ts:806-840. approvals.mdx + internal approval-workflows.md; external expiry sweep requirement thinly covered.                                                                                                    |
| Runtime: idempotency (IdempotencyStore interface, key short-circuit, caches success AND failure)                                                     | partial        | partial                         | runtime-engine.ts:513-519, 2266-2281. Mentioned across pages in both sets (grep verified: spec/semantics.md, runtime-engine.md, several mintlify pages) but no dedicated coverage, and ~~NO built-in implementation ships~~ — caller-provided only, not clearl… **Update (2026-07-14): FIXED (WS2B) — `src/manifest/idempotency/stores/{memory,postgres}.ts` now ship as built-in implementations, exported as `./idempotency/memory` and `./idempotency/postgres`.** |
| Runtime: deterministic mode + provenance/IR-hash verification                                                                                        | partial        | full                            | runtime-engine.ts:3744-3752, 1515-1587. embedded-runtime.mdx touches deterministic generateId/now; effect-boundary errors and requireValidProvenance defaults covered in internal spec only.                                                                |
| Runtime: middleware pipeline (4 hooks, contextPatch, shortCircuit)                                                                                   | full           | full                            | runtime-engine.ts:96-154. runtime-middleware.mdx is the most accurate mintlify page; minor drift: MiddlewareContext.command is the full IRCommand object, doc says string.                                                                                  |
| Runtime: event bus (onEvent, subscribe(entity), getEventLog/clearEventLog)                                                                           | full           | full                            | runtime-engine.ts:4517-4555. In-process only — SSE limitation documented. quickstart's runtime.on() is phantom.                                                                                                                                             |
| Runtime: job queue (MemoryJobQueue, drainJobs, MISSING_JOB_QUEUE)                                                                                    | full           | full                            | runtime-engine.ts:567-596, 2797. async-commands pages in both sets.                                                                                                                                                                                         |
| Runtime: WASM expression evaluator (optional accelerator + TS fallback)                                                                              | missing        | missing                         | src/manifest/wasm/, RuntimeOptions.wasmEvaluator. No .wasm artifact exists in the repo (files[] lists a glob that matches nothing); loader returns null in Node. Only trace in docs is a peer-dep table row (installation.mdx:90). Effectively undocumente… |
| Runtime: profiling (ProfileCollector) + debug command trace (CommandTraceRecorder)                                                                   | partial        | missing                         | src/manifest/profiling.ts, debug/command-trace.ts; ./profiling and ./debug exported. runtime-tooling.mdx instead documents phantom RuntimeProfiler/'@angriff36/manifest/profiler' and TimeTravelDebugger. Real APIs undocumented in both.                   |
| Extensibility: plugin API (projections, store adapters, audit sinks, builtins, CLI commands)                                                         | full           | full                            | plugin-api.ts; ./plugin-api exported. plugin-api.mdx + docs/features/plugin-api.md accurate incl. reserved names and BUILTIN_STORE_TARGETS (which custom-stores.mdx contradicts).                                                                           |
| Extensibility: plugin loader wiring (loadPlugins)                                                                                                    | partial        | partial                         | plugin-loader.ts:284 implemented+tested but called NOWHERE in the CLI or runtime construction — plugin-contributed projections/commands/builtins never auto-activate. Docs present loadPlugins as programmatic (OK) but configuration.mdx overclaims CLI l… |
| Extensibility: Agent SDK (AgentRuntime, Anthropic/OpenAI/Vercel tool defs, intent mapper)                                                            | full           | full                            | src/manifest/agent-sdk/; ./agent-sdk exported. agent-sdk.mdx + docs/features/agent-sdk.md; export names not individually diffed.                                                                                                                            |
| Extensibility: MCP server (compile/execute/validate/explain + 3 resources)                                                                           | full           | full                            | packages/mcp-server/src/server.ts:14-25. mcp-server.mdx + docs/features/mcp-server.md.                                                                                                                                                                      |
| Extensibility: LSP server package (manifest-lsp bin)                                                                                                 | partial        | missing                         | packages/lsp-server; shipped inside @angriff36/manifest. dev-tools.mdx mentions it briefly; no internal page. dist not committed — built by prepublishOnly.                                                                                                 |
| Extensibility: VS Code extension (syntax, snippets, own bundled LSP)                                                                                 | partial        | missing                         | packages/vscode-extension (committed .vsix, build-from-source). dev-tools.mdx notes no marketplace. Neither set flags that it ships a DUPLICATE language server rather than using packages/lsp-server.                                                      |
| Extensibility: federation (registry, client, HTTP transport, policy bridge)                                                                          | partial        | full                            | src/manifest/federation/ real code, NO ./federation export subpath (imports fail). mintlify federation.mdx wrong on nearly every API (phantoms recorded); internal docs/features/federation.md accurately flags the non-export.                             |
| Tooling: binary IR (packIR/unpackIR via ./binary-ir), IR diff, breaking-change detection                                                             | partial        | partial                         | binary-ir.ts:61-78, ir-diff.ts, breaking-change.ts. diff/breaking well covered via CLI pages; binary IR itself surfaces only as the phantom 'compression' module in ir-version-control.mdx. CLI pack/unpack commands are unregistered (pack-unpack.ts unwi… |
| Tooling: IR version store (snapshots/tags/changelog/verify)                                                                                          | full           | full                            | ir-version-store.ts; ./ir-version-store exported; CLI `manifest versions`. Both sets cover it (mintlify page also hosts the compression phantoms).                                                                                                          |
| Tooling: registry emit (commands.json/entities.json inventories)                                                                                     | full           | full                            | registry/emit.ts; CLI `manifest emit registries` (index.ts:788-798). governance.mdx + cli.md; validated against docs/spec/registry schemas.                                                                                                                 |
| Packages: @manifest/stdlib (13 reusable .manifest fragments)                                                                                         | missing        | missing                         | packages/stdlib exists but is NOT published and NOT in @angriff36/manifest files[] — external consumers cannot `use` it. Undocumented in both sets; its own src/index.ts documents an import path that only works in-repo.                                  |
| Packages: pnpm workspace + npm distribution (OIDC publish, exports map)                                                                              | full           | full                            | pnpm-workspace.yaml; package.json exports verified subset in installation.mdx; internal packages-and-distribution.md current. installation.mdx version string stale (2.18.0 vs 2.22.0).                                                                     |
| Docs infra: internal doc-set hygiene (FEATURE-LIST, v0.3.8 migration, dead cross-links)                                                              | missing        | partial                         | Not a code feature but audit-relevant: docs/FEATURE-LIST.md presents shipped v1.9-2.0 items as future; docs/guides/migration/v0.3.8.md is an internal audit in a user path referencing moved files; docs/spec/README.md + performance.md contain dead link… |

**Update (2026-07-14):** spot-checked against current source rather than re-verifying all 143 rows. **FIXED-SINCE:** the "Config: schema's closed projections set" row (now 28 projection keys, not 4, verified directly against `docs/spec/config/manifest.config.schema.json`); the "Runtime: batched persistence" row's "no mintlify coverage of the atomicity guarantee" claim (now documented at `mintlify/language/commands.mdx:121`); the "Extensibility: federation" row's "NO ./federation export subpath" claim (now exported, verified against `package.json`); the "Language: entity inheritance + mixin composition" row's mintlify mixin-syntax mismatch (`advanced-entities.mdx` now shows `mixin` inside the entity header, matching real grammar). **STILL-OPEN, verified unchanged:** the "Language: behaviors" row — `IREntity` still has no `behaviors` field and `ir-compiler.ts` still never reads it; still silently dropped with no diagnostic (grep-verified against current `ir.ts`/`ir-compiler.ts`). The "Projections: Health check" row's "documented NOWHERE in either set" claim is still true — no health-projection page exists in mintlify or `docs/` today. `docs/FEATURE-LIST.md` (mid-edit on disk per `git status` at the time of this pass) is being actively reconciled; a 2026-07-14 note was inserted acknowledging the roadmap snapshot is stale for later-shipped projections, but the underlying Shipped/Unreleased table structure has not yet been rewritten.

## Appendix D — .automaker completion-claim forensics (2026-07-01)

All 116 features in .automaker/features were verified against current main by independent agents (evidence-required; spot-checked). Tally: **working:59, broken:1, partial:38, phantom:16, not-started:2**.

**16 features carry status "verified" with no implementation at all** (phantom). This is the root cause of several phantom doc pages: e.g. multi-environment-config (never built) is exactly what mintlify/cli/configuration.mdx documents as MANIFEST_ENV overlays + env() expansion. The .automaker feature.json status fields for the phantom/partial rows are unreliable and should not be trusted as a feature inventory; this appendix supersedes them.

| Verdict     | Feature                           | Claimed   | Reality                                                                                                                                                                                                                                                                                                      |
| ----------- | --------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| phantom     | consent-tracking                  | verified  | Status is 'verified' but no implementation exists: no consent-related TypeScript files found in src/manifest/, no consent keyword in lexer.ts, no IR types, and the feature.json has no summary — the agent-output.md shows only codebase exploration and unanswered design questions.                       |
| phantom     | contract-testing                  | verified  | No verify-contracts command, no pact files, and no implementation code exists anywhere in the repo; agent-output.md shows only exploration with no commits.                                                                                                                                                  |
| phantom     | cqrs-read-model                   | verified  | The IR interface has no readModels field, the compiler emits nothing for readModel declarations, the runtime has no read-model executor, and the conformance tests for fixture 86 are pre-existing failures.                                                                                                 |
| phantom     | generic-entity-types              | verified  | The parser has no generic entity syntax — fixture 84 is a pinned parse-failure test (shouldFail:true, 'Expected {, got <'), not a passing implementation.                                                                                                                                                    |
| phantom     | interactive-tutorial-mode         | verified  | TutorialPanel.tsx and tutorials/ engine were written but are never exported from artifacts/index.ts and are not wired into App.tsx — the UI has no tutorial tab and the component is unreachable.                                                                                                            |
| phantom     | manifest-import-system            | verified  | Claims 'import' keyword, ImportNode, and parseImport were added, but none of these exist in the actual codebase — 'import' is absent from the KEYWORDS set and no parse path for import declarations was ever created.                                                                                       |
| phantom     | manifest-registry-server          | verified  | Status is 'verified' but the feature.json has no summary, no packages/registry/ directory exists, and no CLI command or HTTP server code was written.                                                                                                                                                        |
| phantom     | multi-environment-config          | verified  | No 'profiles' key, MANIFEST_ENV overlay, or --profile CLI flag exists anywhere in the configuration or CLI source — the feature.json has no summary, confirming it was never implemented.                                                                                                                    |
| phantom     | notification-channel              | verified  | No 'notification' keyword, IRNotification type, or parser branch exists anywhere in the compiler or runtime — the feature.json has no summary.                                                                                                                                                               |
| phantom     | npx-init-templates                | verified  | The init command has no template library, no saas/api-only/monorepo/event-sourced templates, and no template registry fetch — the feature.json has no summary.                                                                                                                                               |
| phantom     | pagination-api                    | verified  | No 'pageable' modifier, cursor/offset pagination types, or nextCursor/totalCount constructs exist anywhere in the compiler, parser, IR schema, or runtime — the feature.json has no summary.                                                                                                                 |
| phantom     | policy-enforcement-default-deny   | verified  | No global policyMode compiler/runtime option, no 'manifest lint --policy-strict' CLI flag, and no no-open-commands check exist; the only policyMode in the codebase is a Convex-projection-local option unrelated to the described feature.                                                                  |
| phantom     | runtime-metrics                   | verified  | No metrics implementation exists; the agent only wrote a plan and never created any files.                                                                                                                                                                                                                   |
| phantom     | runtime-time-travel               | verified  | Both claimed files (time-travel.ts and TimeTravelPanel.tsx) do not exist; only a pre-existing CommandTraceRecorder utility is present.                                                                                                                                                                       |
| phantom     | soft-delete-pattern               | verified  | The softDelete language keyword was planned but never implemented; only a pre-existing prisma-store config option exists.                                                                                                                                                                                    |
| phantom     | transactional-outbox              | verified  | The entire implementation (TransactionContext interface, TransactionalStore upgrade, beginTransaction, transactionProvider option, rollback/commit lifecycle) is absent from runtime-engine.ts and stores.node.ts; the test file exists but it explicitly tests the non-transactional gap and states the fea |
| broken      | analytics-projection              | verified  | AnalyticsProjection is registered and the ./projections/analytics export exists, but the generated analytics.events.ts calls SDK functions (analytics.track etc.) without importing any SDK, and imports <Entity>AnalyticsContext from a sibling module no surface generates — output cannot compile.        |
| partial     | approval-workflow                 | completed | The approval gate (lexer/parser/IR/runtime lifecycle) is wired end-to-end and fixture 68 passes, but expireApprovals() has zero callers so timed-out approvals never expire, and the PostgresApprovalStore SQL schema is absent from the npm files list.                                                     |
| partial     | async-command-execution           | completed | The async keyword, JobQueue interface, MemoryJobQueue, and drainJobs() work end-to-end for testing, but no durable PostgresJobQueue ships and no drain worker exists — production async commands are blocked without hand-implementing a queue.                                                              |
| partial     | bundle-size-analyzer              | verified  | analyze.ts and its test file exist in commands/ and are internally functional, but analyzeCommand is never imported or registered in packages/cli/src/index.ts, making the manifest analyze command unreachable — confirmed by the reconciliation audit (Cluster C item 15: 'analyze' is one of 5 unregister |
| partial     | custom-expression-functions       | completed | Direct RuntimeOptions.customBuiltins injection works, but the plugin-config auto-loading pathway (loadPlugins) has zero call sites and is inert.                                                                                                                                                             |
| partial     | custom-store-adapter              | completed | IRStore.target widened and BUILTIN_STORE_TARGETS added, but the plugin-based StoreAdapterPlugin registration pathway is inert (loadPlugins never called); only the pre-existing direct storeProvider callback works.                                                                                         |
| partial     | dynamodb-store-adapter            | verified  | DynamoDBStore class and infrastructure projection exist and are accessible, but 'dynamodb' is absent from BuiltinStoreTarget (contradicting the feature claim), DynamoDBOutboxStore is not exported, and runtime auto-wiring requires inert plugin path.                                                     |
| partial     | event-sourcing-projection         | verified  | IR accepts 'eventSourced' as a passthrough string target but the claimed EventSourcedStore runtime class was never added — runtime store is absent.                                                                                                                                                          |
| partial     | express-projection                | completed | ExpressProjection is registered in the CLI registry and reachable, but is absent from package.json exports and generated code imports runtime helpers that no projection ever emits.                                                                                                                         |
| partial     | feature-1780387482210-qhzhvc02q0j | verified  | Health projection is registered in builtins (CLI reachable), but the explicit goal of adding ./projections/health to package.json exports was not completed.                                                                                                                                                 |
| partial     | flutter-projection                | verified  | DartProjection exists and is CLI-reachable via the registry, but is absent from package.json exports and its generated Dart client targets REST routes no server projection emits.                                                                                                                           |
| partial     | graphql-projection                | completed | GraphQLProjection exists, is CLI-reachable, and has 41 tests, but ./projections/graphql is absent from package.json exports and resolvers are scaffolding-only stubs.                                                                                                                                        |
| partial     | health-check-endpoint             | verified  | Projection is registered and reachable, but every generated health check is a TODO stub that always returns 'healthy' regardless of real store state.                                                                                                                                                        |
| partial     | hono-projection                   | verified  | Hono projection is registered and reachable, but its signature advertised option (basePath) is normalized and then never applied to any generated route string.                                                                                                                                              |
| partial     | ir-compression                    | verified  | The binary-ir library is implemented and exported in package.json, but the CLI pack/unpack commands are not registered in index.ts and are unreachable from the command line.                                                                                                                                |
| partial     | kysely-projection                 | verified  | Kysely projection is registered and reachable, but the advertised columnMappings option is dead code that is normalized but never consulted during code generation.                                                                                                                                          |
| partial     | materialized-view-projection      | verified  | Projection exists, is registered in builtins.ts, and generates DDL — but the central claim that IR expressions are translated to SQL via expression-to-sql.ts is false: generator.ts never imports that translator and all view column SQL must be hand-authored by the user.                                |
| partial     | migration-cli-integration         | completed | The migrate command is registered and accepts the documented flags, but both Prisma and Drizzle migration invocations print 'not implemented' — no actual migration is ever applied.                                                                                                                         |
| partial     | natural-language-to-manifest      | verified  | generate-from-prompt.ts exists with LLM integration logic, but it is never registered in the CLI index.ts and is therefore dead code — no user can reach it via 'manifest generate --from-prompt'.                                                                                                           |
| partial     | performance-profiler              | verified  | Runtime instrumentation exists and is wired, but the CLI 'profile' command is not registered in packages/cli/src/index.ts and FlameGraphPanel.tsx is not integrated in ArtifactsPanel.tsx — two of three claimed deliverables are unreachable dead code.                                                     |
| partial     | plugin-api                        | verified  | plugin-api.ts and plugin-loader.ts exist and are exported in package.json, but loadPlugins() has zero call sites in the CLI or runtime — the 'plugins:' config key is parsed but plugins are never actually loaded.                                                                                          |
| partial     | policy-matrix-viewer              | verified  | PolicyMatrixPanel.tsx exists at 575 lines but is not imported in ArtifactsPanel.tsx and has no panel mode routing — the component is dead code unreachable in the UI.                                                                                                                                        |
| partial     | python-pydantic-projection        | verified  | Projection is registered in builtins.ts and generates real Python code, but clientBaseUrl, clientClassName, and emitConvenienceFunctions options are defined in types.ts and never referenced in generator.ts — no pydantic.client surface exists despite the interface implying one.                        |
| partial     | react-query-projection            | verified  | Projection is registered and generates typed hooks, but the advertised 'optimisticUpdates' option is defined and defaulted to true in the options type yet is never referenced in any code-generation logic — it is dead code.                                                                               |
| partial     | realtime-subscription             | verified  | The 'realtime' contextual modifier compiles to IR, and the Next.js projection generates SSE routes and React hooks, but subscription delivery is single-process in-memory only — no cross-instance pub/sub and no eventBus RuntimeOption.                                                                    |
| partial     | redis-store-adapter               | verified  | RedisStore for entity persistence is accessible via the './stores' export, but RedisOutboxStore in outbox/stores/redis.ts is missing from package.json exports — the outbox Redis adapter is unreachable by package consumers.                                                                               |
| partial     | remix-projection                  | verified  | RemixProjection is registered in builtins.ts and generates real loader/action code across 5 surfaces, but the generated code requires hand-authored runtime factory companions (createManifestRuntime, auth middleware) that no projection emits — a systemic gap confirmed by the audit for all server proj |
| partial     | runtime-federation                | verified  | Federation module is fully implemented and tested but missing from package.json exports so no user can import it.                                                                                                                                                                                            |
| partial     | scheduled-command                 | verified  | Schedule IR, parser, and manual runSchedule() are real, but ~~shouldRunCron() is hardcoded to return false so automated cron evaluation never fires.~~ **Update (2026-07-14): FIXED (WS2B) — `shouldRunCron` (`runtime-schedule.ts:182-184`) now delegates to a real cron-field evaluator, verified directly; this row is now `working`, not `partial`.**                                                                                                                                                          |
| partial     | sdk-python                        | verified  | Pydantic projection generates real code and is registered, but multiple documented options (clientBaseUrl, clientClassName, emitConvenienceFunctions, customImports) are typed but never consulted.                                                                                                          |
| partial     | seed-data-generator               | verified  | Seed generator and tests exist in packages/cli/src/commands/ but the manifest seed command is never registered in index.ts.                                                                                                                                                                                  |
| partial     | standard-library                  | verified  | stdlib package exists in the pnpm workspace with real .manifest files but has never been published to npm and cannot be installed by users.                                                                                                                                                                  |
| partial     | sveltekit-projection              | verified  | SvelteKit projection generates real server routes and is registered, but documented options are partially phantom and generated code requires a hand-authored runtime factory.                                                                                                                               |
| partial     | tenant-isolation-policy           | verified  | Core isolation logic is wired end-to-end but the two claimed conformance fixtures (58-tenant-isolation.manifest and 58-tenant-isolation.ir.json) are absent, and Prisma RLS is emitted as comments not real SQL migrations.                                                                                  |
| partial     | timestamp-auto-fields             | verified  | Runtime auto-population of createdAt/updatedAt is implemented but the two claimed conformance fixtures (59-timestamp-auto-fields.manifest and expected JSON) are absent.                                                                                                                                     |
| partial     | turso-store-adapter               | verified  | TursoStore class is implemented and exported but 'turso' was not added to BuiltinStoreTarget in ir.ts or BUILTIN_STORE_TARGETS in plugin-api.ts, so users cannot use 'store Entity in turso' as a first-class builtin and must manually wire via storeProvider.                                              |
| partial     | vscode-extension                  | verified  | Extension code is fully built with LSP server, grammar, snippets, and three .vsix artifacts, but it is not published to the VS Code marketplace; install requires the manual VSIX path, contradicting the claim of 'one-click install from marketplace'.                                                     |
| partial     | wasm-runtime                      | verified  | TypeScript wrapper code and AssemblyScript source exist and the ./wasm export is in package.json, but no compiled .wasm binary exists anywhere in the repo so the WASM fast-path is always a no-op and only the TypeScript fallback runs.                                                                    |
| partial     | webhook-trigger                   | verified  | Webhook declarations parse to IR correctly but there is no runtime handling and no HTTP route surfaces in any projection; the feature.json itself says projection work was explicitly deferred.                                                                                                              |
| not-started | cross-entity-actions              | backlog   | feature.json status is explicitly 'backlog' with no implementation attempted.                                                                                                                                                                                                                                |
| not-started | openapi-projection                | backlog   | Feature.json status is 'backlog' and no automaker claim was made; the OpenAPI projection itself is pre-existing (shipped in v1.0.23) but was not attributed to this automaker feature.                                                                                                                       |
| working     | aggregate-computed-properties     | completed | avg, min_of, max_of, count_of, filter, map aggregate builtins exist in the runtime and conformance fixture 64 verifies end-to-end execution.                                                                                                                                                                 |
| working     | ai-agent-sdk                      | verified  | All 7 agent-sdk source files exist under src/manifest/agent-sdk/ and the ./agent-sdk subpath is exported from package.json, making it reachable by package consumers.                                                                                                                                        |
| working     | ai-test-generator                 | verified  | The gen-tests command file exists and is registered in the CLI via alias at index.ts:375 with dynamic import at line 389.                                                                                                                                                                                    |
| working     | array-type                        | verified  | Array property type (T[] and array<T> syntax) is implemented in the parser, IR, Zod projection, and Prisma projection; conformance fixture 40 verifies compilation — though no summary exists in feature.json, the capability is functional on main.                                                         |
| working     | breaking-change-detector          | completed | breaking-change.ts CLI command file exists, breakingChangeCommand is imported and registered in index.ts, and ./breaking-change is exported from package.json — fully reachable.                                                                                                                             |
| working     | changelog-from-ir-diff            | completed | changelog.ts and changelog.test.ts exist, changelogCommand is imported in index.ts:54 and the .command('changelog') is registered at line 910, reachable as manifest changelog.                                                                                                                              |
| working     | ci-github-actions                 | completed | init-ci.ts exists, initCiCommand is imported in index.ts:23, and the --ci option is wired to the init command at lines 120-124 invoking initCiCommand(options.ci, ...).                                                                                                                                      |
| working     | command-coverage-report           | completed | coverage.ts and its test file exist, coverageCommand is imported at index.ts:33, and .command('coverage') is registered at line 728 — fully reachable as manifest coverage.                                                                                                                                  |
| working     | command-retry-policy              | verified  | retry block is in the IR (IRRetry at ir.ts:247-256, retry?: IRRetry at :413), the runtime retry loop is implemented, and conformance fixture 72 verifies end-to-end compilation and execution.                                                                                                               |
| working     | computed-property-caching         | completed | cache/request/session/ttl keywords are added to the lexer, IRComputedPropertyCache appears twice in ir.ts, and conformance fixture 65 verifies the cache annotation compiles and the runtime evaluates cached values correctly.                                                                              |
| working     | constraint-test-harness           | verified  | ConstraintTestPanel is fully wired: component, runtime method, panel tab, and export all exist.                                                                                                                                                                                                              |
| working     | cross-entity-constraint           | verified  | Relationship traversal in constraints is fully implemented: _entity metadata, generalized member access, and conformance fixture 70 all present.                                                                                                                                                             |
| working     | data-masking                      | verified  | Read-time masking is fully implemented across lexer, parser, IR, runtime, conformance fixture, and unit tests.                                                                                                                                                                                               |
| working     | date-time-types                   | verified  | date, time, datetime, and duration types are implemented with helpers, runtime builtins, and conformance fixture 92.                                                                                                                                                                                         |
| working     | decimal-type                      | verified  | decimal and money types are lexed, parsed, lower to IR with precision/scale params, and covered by conformance fixture 56.                                                                                                                                                                                   |
| working     | declarative-event-reactions       | verified  | Reaction rules are fully compiled to IR, executed in the runtime with depth-guard, and covered by conformance fixture 67.                                                                                                                                                                                    |
| working     | documentation-site-generator      | completed | manifest docs command is fully registered in the CLI, imports @angriff36/manifest/ir-compiler, and has 16 unit tests.                                                                                                                                                                                        |
| working     | drizzle-projection                | completed | DrizzleProjection is registered in builtins, exported from projections/index, and has a dedicated package.json export path.                                                                                                                                                                                  |
| working     | entity-inheritance                | verified  | Entity extends/mixin composition is fully implemented via expandEntityComposition, called in the IR compiler, with 5 conformance fixtures covering both happy-path and error cases.                                                                                                                          |
| working     | enum-type                         | verified  | Enum types are fully wired from lexer through IR and into the conformance suite.                                                                                                                                                                                                                             |
| working     | environment-variable-mapping      | verified  | manifest preflight command is implemented and registered in the CLI.                                                                                                                                                                                                                                         |
| working     | event-subscription-language       | completed | Reaction syntax (on/run/resolve/params) is fully wired through lexer, parser, IR, and runtime dispatch.                                                                                                                                                                                                      |
| working     | expression-language-extensions    | verified  | 27 new expression built-in functions (string, numeric, array, date) are present in the runtime and covered by fixture 56.                                                                                                                                                                                    |
| working     | feature-1780206660992-92bdiex42j7 | verified  | Canonical event.subject metadata is fully implemented: EventSubject type, deterministic resolution chain, and outbox preservation all present.                                                                                                                                                               |
| working     | feature-1780316518102-h71n1r2u1fm | verified  | manifest watch command exists and is registered in the CLI.                                                                                                                                                                                                                                                  |
| working     | feature-flags-integration         | completed | flag() builtin and flagProvider RuntimeOption are implemented and documented; safe-default (returns false) is wired.                                                                                                                                                                                         |
| working     | field-level-encryption            | verified  | The encrypted modifier is wired through lexer/parser/IR/runtime with EncryptionProvider at 4 store boundary points; silent no-op without a provider is a documented design choice (audit flags it as a philosophy concern, not a broken claim).                                                              |
| working     | full-text-search                  | verified  | searchable modifier and search() built-in are wired through lexer/parser/IR/runtime and Prisma/Drizzle projections emit correct index syntax.                                                                                                                                                                |
| working     | ir-diff-tool                      | completed | IR diff commands (source-vs-ir, ir-vs-ir, breaking) are fully implemented, imported, and registered in the CLI index.                                                                                                                                                                                        |
| working     | ir-graph-visualizer               | verified  | IRGraphPanel is implemented, exported from artifacts/index.ts, and wired into App.tsx under the 'graph' tab.                                                                                                                                                                                                 |
| working     | ir-to-mermaid                     | completed | manifest diagram command is implemented and registered in the CLI, generating Mermaid ER/state-machine/sequence diagrams from IR.                                                                                                                                                                            |
| working     | ir-version-control                | verified  | All eight IR version sub-commands (list, show, save, diff, changelog, tag, rollback, verify) are implemented in versions.ts and fully registered in the CLI.                                                                                                                                                 |
| working     | json-schema-projection            | verified  | JSON Schema projection is implemented (directory named 'jsonschema'), registered in builtins.ts, and reachable via manifest generate --projection jsonschema.                                                                                                                                                |
| working     | language-server-protocol          | verified  | LSP server package is fully implemented with all five LSP features (diagnostics, hover, completion, definition, document-symbols), compiled to dist/, and listed in the root package files allowlist.                                                                                                        |
| working     | llm-context-export                | verified  | LLM context projection is implemented, registered in builtins.ts, and reachable via manifest generate --projection llm-context through the registry path.                                                                                                                                                    |
| working     | llm-ir-validator                  | verified  | manifest validate-ai command is implemented with eight supporting modules and is registered in the CLI index.                                                                                                                                                                                                |
| working     | load-testing-fixtures             | verified  | manifest load-test command is implemented in load-test.ts with a test file, and is registered in the CLI index.                                                                                                                                                                                              |
| working     | manifest-format                   | verified  | manifest fmt command is implemented with fmt.ts and fmt.test.ts, and is fully registered in the CLI with --write flag support.                                                                                                                                                                               |
| working     | manifest-mcp-server               | verified  | MCP server package exists at packages/mcp-server/ with all four claimed tools (compile, execute, validate, explain), is wired as a binary in package.json, and has test coverage.                                                                                                                            |
| working     | manifest-playground               | verified  | Playground HTML entry and all React components exist as a second Vite entry point with URL-hash sharing and LZ-string compression.                                                                                                                                                                           |
| working     | map-type                          | verified  | Map<K,V> property type is wired in the lexer, Prisma/Drizzle type mappings, runtime engine map methods, and a conformance fixture was added.                                                                                                                                                                 |
| working     | mock-server                       | verified  | Mock HTTP server command exists, is registered in the CLI, uses real RuntimeEngine for command execution, derives routes from IR, and has 21 tests.                                                                                                                                                          |
| working     | multi-module-compilation          | verified  | 'use' keyword, UseNode, multi-compiler.ts, module-resolver.ts all exist and the --merge CLI flag is registered.                                                                                                                                                                                              |
| working     | pre-commit-hooks                  | backlog   | Feature.json claims 'backlog' (not started) but installHooksCommand is fully implemented and live-registered in the CLI index as 'manifest install-hooks'.                                                                                                                                                   |
| working     | property-based-testing            | verified  | fast-check is installed and three property-test files using fc.property are integrated into the vitest suite, covering expression evaluation, constraint, and builtin properties.                                                                                                                            |
| working     | range-constraint                  | verified  | min/max/between/length builtins are fully wired in the runtime engine, constraint-analysis.ts provides static analysis, Zod and Prisma projections consume the analysis, and conformance fixtures 56-57 provide executable evidence.                                                                         |
| working     | rate-limiting-policy              | verified  | rateLimit blocks compile to IRRateLimit in the IR and are enforced at runtime via a RateLimiter class checked before command execution and in policy evaluation.                                                                                                                                             |
| working     | regex-constraint                  | verified  | matches() builtin is in the runtime engine, compile-time regex validation is in ir-compiler.ts, PatternConstraint is in constraint-analysis.ts, Zod integration exists, and conformance fixture 63 provides executable evidence.                                                                             |
| working     | role-hierarchy                    | verified  | Role declarations compile through parser → ir-compiler (transformRole/resolveRoleGraph) → runtime (buildRoleIndex/roleHasPermission), with hasPermission() and roleAllows() builtins, all verified by conformance fixture 71.                                                                                |
| working     | runtime-middleware                | verified  | Middleware pipeline is fully implemented, wired into runCommand, and exported via the main package entry.                                                                                                                                                                                                    |
| working     | runtime-repl                      | verified  | manifest repl is registered in the CLI and backed by a 909-line readline-based implementation.                                                                                                                                                                                                               |
| working     | saga-workflow                     | verified  | Saga orchestration is fully implemented in the IR, runtime engine, and conformance suite.                                                                                                                                                                                                                    |
| working     | search-projection                 | verified  | Elasticsearch projection generates index mappings, is registered in builtins, has 389-line test suite, and is reachable via the ./projections registry export.                                                                                                                                               |
| working     | snapshot-testing                  | verified  | Snapshot tests for all built-in projection generators exist and are backed by a real .snap file.                                                                                                                                                                                                             |
| working     | storybook-projection              | verified  | Storybook CSF projection is fully implemented, registered, and has a 402-line test suite.                                                                                                                                                                                                                    |
| working     | terraform-projection              | verified  | TerraformProjection is implemented, registered as a builtin, and generates valid HCL from IR; multi-file output (database.tf, storage.tf, messaging.tf) is always on rather than a configurable option, and the surface is 'terraform.hcl' not 'terraform.main' as docs claim.                               |
| working     | value-object-type                 | verified  | Value object declarations compile end-to-end: parser, IR, code generator, and Prisma JSONB mapping are all wired, and the conformance fixture exists.                                                                                                                                                        |
| working     | watch-mode-compiler               | verified  | 'manifest watch' command is fully implemented and registered in the CLI with debounce, structured JSON events, and incremental rebuild.                                                                                                                                                                      |
| working     | zod-schema-projection             | verified  | ZodProjection is fully implemented, registered as a builtin, generates z.object() schemas for entities and commands, and has a test suite.                                                                                                                                                                   |

## Appendix E — language gaps surfaced while fixing doc snippets (2026-07-01)

Fixing the 31 non-compiling snippets exposed places where the docs expressed reasonable intent the language rejects. Each was verified against the compiler; docs were rewritten to legal syntax, and the gap is recorded here rather than papered over:

| Gap                            | Detail                                                                                                                                                                                                                                                                                                              |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `map<K, V>` two-parameter form | Parser accepts only `map<V>` (keys implicitly string). The documented `record<K, V>` alias does not exist either.                                                                                                                                                                                                   |
| `retry` block fields           | `initialDelay`/`maxDelay`/`attempts` do not exist; real fields are `maxAttempts`, `backoff`, `delay` (fixture 72). No delay-cap field exists.                                                                                                                                                                       |
| `rateLimit` block fields       | `perMinute`/`strategy: fixed\|sliding` do not exist; real fields are `maxRequests`, `windowMs`, `scope` with bare-identifier scopes (`user`, `tenant`, `global`) — `scope: user.id` is invalid (fixture 74).                                                                                                        |
| Reserved-word ergonomics       | `publish`, `read`, `execute`, `delete`, `to`, `tenant`, `self`, `void`, `in` cannot be identifiers — so a command cannot be named `publish`, a role permission cannot be named `delete`, a relationship cannot be named `tenant`. Common domain words; worth considering contextual keywords or quoted identifiers. |
| Command-body policies          | No inline `policy` inside a command body; per-command restrictions require guards. Entity `default policy` + guards is the only layering mechanism (no per-command policy override).                                                                                                                                |
| String member `.length`        | `title.length` does not resolve at runtime; the builtin is `length(v)`.                                                                                                                                                                                                                                             |
| `timestamp` type               | Not a type name (docs used it); real options are `number` or `date`/`datetime`.                                                                                                                                                                                                                                     |

## Corrections found during WS2C implementation (2026-07-02)

Two audit claims were refuted during implementation (recorded per the verify-and-fix-source discipline):

1. **"Implemented adapters unreachable via package exports" overstated test coverage.** The DynamoDB and MongoDB outbox stores (src/manifest/outbox/stores/{dynamodb,mongodb}.ts) have real implementations but **zero test files** — the audit's "fully implemented ... adapters ... exist in source" framing implied all three were tested; only the Redis outbox store is (live-gated suite in stores.node.test.ts). Consequently only `./outbox/redis` and `./federation` were exposed in WS2C; DynamoDB (which also has a constructor-vs-docs mismatch and an unverified claim() status question) and MongoDB stay unexported until they have behavioral tests.
2. **"Plugin projections" is not a distinct plugin type.** The plugin API has StoreAdapterPlugin/AuditSinkPlugin/BuiltinFunctionPlugin/CliCommandPlugin wrapper types but no ProjectionPlugin — plugins contribute projections directly as `ManifestPlugin.projections: ProjectionTarget[]`, which the loader registers via registerProjection. The capability is real and (as of WS2C) wired; the type taxonomy in the audit item was imprecise.
